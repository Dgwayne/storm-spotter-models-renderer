# Canonical product catalog for storm-spotter-models-renderer.
#
# Each product has:
#   wgrib2_match:   regex passed to `wgrib2 -match` to select the GRIB message(s)
#   units_in:       native GRIB units (for documentation / sanity)
#   units_out:      display units after gdal/wgrib2 conversion
#   convert:        optional gdal_calc expression to convert raw -> display units
#                   placeholders: A = raw value
#                   (left blank = identity; gdaldem ramps work on raw values then)
#   clr:            color table file under config/color_tables/
#   pal:            matching .pal asset name (for the Flutter app)
#   display:        human-readable name shown in the model picker
#   point_values:   when true, publish a per-frame F###.json value grid next to
#                   the PNG (sample_point_values.py) for on-map numbers
#   point_decimals: decimals to keep in the published values (default 0)
#   point_value_fh_cap: publish that F###.json for fh <= N only, while the PNG
#                   still renders the full forecast range. build_manifest.py
#                   then reports pointValues false in productCatalog (the
#                   models tab would scrub off the end of the grids) and true
#                   in mesoCatalog. Independent of fh_cap, which caps the PNG
#                   too. NO CURRENT PRODUCT USES IT — srh/ehi did for a few
#                   hours on 2026-07-27 until the crosshair inspector needed
#                   grids at every scrubbed hour; kept for the next product
#                   whose grid is only ever read at one hour.
#   gpu_data:       {min, max} — when present, ALSO bake a value-encoded data
#                   PNG next to the colored one (F###.data.png): gray 1..255 =
#                   min..max linear in DISPLAY units, 0/alpha-0 = nodata. The
#                   app's GPU model layer samples it and colorizes in a shader
#                   (crisp at any zoom, like the MRMS data_png path). The
#                   colored PNG stays the back-compat/fallback frame; released
#                   apps ignore gpuDataMin/Max and keep rendering it.
#
# Models declare which products they publish in the `models:` map at the bottom.
#
# ECMWF (index_format: ecmwf) matches against the open-data JSON index
# instead of a wgrib2 idx: `ecmwf_match` is a regex over the canonical
# "param:levtype:levelist" key built per index line (levelist empty for
# sfc), e.g. "^2t:sfc:" or "^(u|v):pl:500$". Matched messages are fetched
# in param-alphabetical order, which keeps U before V for composite_uv.

products:
  refc:
    display: "Composite Reflectivity"
    group: "Surface & Precipitation/Radar"
    # No trailing colon: HRRR/GFS idx lines say ":REFC:entire atmosphere:"
    # but RRFS writes ":REFC:entire atmosphere (considered as a single
    # layer):" — the prefix matches all three, still a single message each.
    wgrib2_match: ":REFC:entire atmosphere"
    units_in: "dBZ"
    units_out: "dBZ"
    convert: ""
    clr: "refc.clr"
    pal: "hrrr_refc.pal"
    gpu_data: { min: -30.0, max: 80.0 }
    # Smooth, interpolated color gradient (no -nearest_color_entry) so
    # reflectivity renders like RadarOmega instead of hard 5-dBZ bands.
    # All other products omit this flag and keep crisp binned colors.
    interpolate_color: true

  t2m:
    display: "2m Temperature"
    group: "Surface & Precipitation/Surface"
    wgrib2_match: ":TMP:2 m above ground:"
    # GRIB encodes Kelvin per WMO convention, but GDAL's GRIB driver
    # auto-normalizes temperature to Celsius on read (per the GRIB
    # CF-conformance defaults). Convert Celsius -> Fahrenheit so the
    # .clr ramp (keyed in degF) lines up with the actual data.
    units_in: "C"
    units_out: "degF"
    convert: "A * 1.8 + 32"
    clr: "t2m.clr"
    pal: "hrrr_t2m.pal"
    gpu_data: { min: -60.0, max: 130.0 }
    ecmwf_match: "^2t:sfc:"
    # Open-Meteo data_spatial values are already degC, so the same C -> F
    # convert applies (see the ICOND2 model notes).
    om_var: "temperature_2m"
    point_values: true
    # also_meso: publish in the manifest's mesoCatalog too — the app's
    # Mesoanalysis layer lists this product (its f00 frame already
    # renders for the models tab, so reuse costs nothing).
    also_meso: true

  td2m:
    display: "2m Dewpoint"
    group: "Surface & Precipitation/Surface"
    wgrib2_match: ":DPT:2 m above ground:"
    # GDAL auto-normalizes K -> C, same as t2m. See note there.
    units_in: "C"
    units_out: "degF"
    convert: "A * 1.8 + 32"
    clr: "td2m.clr"
    pal: "hrrr_td2m.pal"
    gpu_data: { min: -60.0, max: 90.0 }
    ecmwf_match: "^2d:sfc:"
    # Open-Meteo ships no 2m dewpoint — derive via Magnus from T (degC) +
    # RH (%). om_calc runs INSTEAD of convert and must emit final display
    # units (degF). RH clamped >= 0.1 so log() never sees zero.
    om_inputs:
      A: "temperature_2m"
      B: "relative_humidity_2m"
    om_calc: "(243.04*(log(maximum(B,0.1)/100.0)+17.625*A/(243.04+A))/(17.625-(log(maximum(B,0.1)/100.0)+17.625*A/(243.04+A))))*1.8+32"
    point_values: true
    also_meso: true

  wind10m:
    display: "10m Wind Speed"
    group: "Surface & Precipitation/Surface"
    # Special: this product needs UGRD + VGRD combined into magnitude.
    # Handled with two-pass wgrib2 + gdal_calc; see decode_pipeline.sh.
    wgrib2_match: ":(UGRD|VGRD):10 m above ground:"
    composite_uv: true
    units_in: "m/s"
    units_out: "kt"
    convert: "A * 1.94384"
    clr: "wind.clr"
    pal: "hrrr_wind10m.pal"
    gpu_data: { min: 0.0, max: 120.0 }
    # Colon anchors the param so "100u:sfc:" can never match.
    ecmwf_match: "^(10u|10v):sfc:"
    om_uv: ["wind_u_component_10m", "wind_v_component_10m"]
    point_values: true
    also_meso: true

  # ── Marine / wave products (om-sourced, batch 3) ─────────────────────
  waveHgt:
    display: "Wave Height"
    group: "Marine"
    wgrib2_match: ""
    om_var: "wave_height"
    units_in: "m"
    units_out: "ft"
    convert: "A * 3.28084"
    clr: "wave.clr"
    pal: ""
    interpolate_color: true
    point_values: true
    point_decimals: 1
  swellHgt:
    display: "Swell Height"
    group: "Marine"
    wgrib2_match: ""
    om_var: "swell_wave_height"
    units_in: "m"
    units_out: "ft"
    convert: "A * 3.28084"
    clr: "wave.clr"
    pal: ""
    interpolate_color: true
    point_values: true
    point_decimals: 1
  wavePeriod:
    display: "Wave Period"
    group: "Marine"
    wgrib2_match: ""
    om_var: "wave_period"
    units_in: "s"
    units_out: "s"
    convert: ""
    clr: "period.clr"
    pal: ""
    interpolate_color: true
    point_values: true
    point_decimals: 1

  wind10mS:
    display: "10m Wind Speed"
    group: "Surface & Precipitation/Surface"
    wgrib2_match: ""
    # Some om models (UKMO, GDPS) publish 10m wind as a SPEED scalar
    # rather than U/V components — same display/ramp as wind10m, distinct
    # code because product keys are global and wind10m is composite_uv.
    om_var: "wind_speed_10m"
    units_in: "m/s"
    units_out: "kt"
    convert: "A * 1.94384"
    clr: "wind.clr"
    pal: ""
    gpu_data: { min: 0.0, max: 120.0 }
    point_values: true

  gust10m:
    display: "10m Wind Gust"
    group: "Surface & Precipitation/Surface"
    wgrib2_match: ":GUST:surface:"
    units_in: "m/s"
    units_out: "kt"
    convert: "A * 1.94384"
    clr: "wind.clr"
    pal: "hrrr_gust10m.pal"
    gpu_data: { min: 0.0, max: 140.0 }
    # ECMWF 10fg = max gust since the previous 3 h step (not instantaneous).
    ecmwf_match: "^10fg:sfc:"
    om_var: "wind_gusts_10m"
    point_values: true
    also_meso: true

  mslp:
    display: "Mean Sea-Level Pressure"
    group: "Surface & Precipitation/Surface"
    # HRRR uses MSLMA, GFS uses PRMSL. NAM carries BOTH PRMSL and MSLET —
    # two messages are fetched and gdal_translate -b 1 takes the first in
    # message order, so NAM renders PRMSL (the standard reduction).
    wgrib2_match: ":(MSLMA|MSLET|PRMSL):mean sea level:"
    ecmwf_match: "^msl:sfc:"
    # Open-Meteo pressure_msl is ALREADY hPa (GRIB sources are Pa) — the
    # explicit empty om_convert suppresses the /100 for om models.
    om_var: "pressure_msl"
    om_convert: ""
    units_in: "Pa"
    units_out: "hPa"
    convert: "A / 100.0"
    clr: "mslp.clr"
    pal: "hrrr_mslp.pal"
    gpu_data: { min: 940.0, max: 1050.0 }
    point_values: true

  sfccape:
    display: "Surface CAPE"
    group: "Severe Weather/Instability"
    wgrib2_match: ":CAPE:surface:"
    om_var: "cape"
    units_in: "J/kg"
    units_out: "J/kg"
    convert: ""
    clr: "cape.clr"
    pal: "hrrr_sfccape.pal"
    gpu_data: { min: 0.0, max: 6000.0 }
    point_values: true
    also_meso: true

  precip1h:
    display: "1-hour Precipitation"
    group: "Surface & Precipitation/Precipitation"
    # 1-hour bucket ending at the current fh: "(fh-1)-fh hour acc".
    # decode_pipeline.sh substitutes {fh} and {fh_minus_1} before matching.
    wgrib2_match: ":APCP:surface:{fh_minus_1}-{fh} hour acc"
    # Open-Meteo `precipitation` = preceding-hour sum (mm), meaningless at
    # f00 (om_fh_min gates om sources only; GRIB models self-gate via the
    # {fh_minus_1} idx match failing at f00).
    om_var: "precipitation"
    om_fh_min: 1
    units_in: "kg/m^2"
    units_out: "in"
    convert: "A / 25.4"
    clr: "precip.clr"
    pal: "hrrr_precip1h.pal"
    gpu_data: { min: 0.0, max: 6.0 }
    point_values: true
    point_decimals: 2

  capeMU:
    display: "CAPE (Most Unstable)"
    group: "Severe Weather/Instability"
    # ECMWF open data publishes MUCAPE (not surface-based) — a separate
    # product code from sfccape so the display name stays honest, and from
    # the meso `mucape` (which is HRRR-analysis-only with fh_cap 1).
    ecmwf_match: "^mucape:sfc:"
    wgrib2_match: ""
    units_in: "J/kg"
    units_out: "J/kg"
    convert: ""
    clr: "cape.clr"
    pal: "hrrr_sfccape.pal"
    gpu_data: { min: 0.0, max: 6000.0 }
    point_values: true

  tccF:
    display: "Cloud Cover"
    group: "Clouds & Satellite"
    # ECMWF-feed twin of tcc. The 0-1-fraction assumption was WRONG:
    # first AIFS render read 0-10020 through the x100 convert (verified
    # 2026-08-06 via F-json), i.e. the open-data GRIB already carries %.
    # Convert dropped, bad frames purged. Kept as a separate code from
    # tcc anyway so a future feed-specific unit surprise stays isolated.
    ecmwf_match: "^tcc:sfc:"
    wgrib2_match: ""
    units_in: "%"
    units_out: "%"
    convert: ""
    clr: "cloud.clr"
    pal: ""
    interpolate_color: true
    point_values: true

  lccF:
    display: "Cloud Cover (Low)"
    group: "Clouds & Satellite"
    ecmwf_match: "^lcc:sfc:"
    wgrib2_match: ""
    # Open-data GRIB carries % already — see the tccF note.
    units_in: "%"
    units_out: "%"
    convert: ""
    clr: "cloudlow.clr"
    pal: ""
    interpolate_color: true
    point_values: true

  mccF:
    display: "Cloud Cover (Mid)"
    group: "Clouds & Satellite"
    ecmwf_match: "^mcc:sfc:"
    wgrib2_match: ""
    # Open-data GRIB carries % already — see the tccF note.
    units_in: "%"
    units_out: "%"
    convert: ""
    clr: "cloudmid.clr"
    pal: ""
    interpolate_color: true
    point_values: true

  hccF:
    display: "Cloud Cover (High)"
    group: "Clouds & Satellite"
    ecmwf_match: "^hcc:sfc:"
    wgrib2_match: ""
    # Open-data GRIB carries % already — see the tccF note.
    units_in: "%"
    units_out: "%"
    convert: ""
    clr: "cloudhigh.clr"
    pal: ""
    interpolate_color: true
    point_values: true

  precipTotalM:
    display: "Total Precipitation"
    group: "Surface & Precipitation/Precipitation"
    # ECMWF tp is a running total in METERS (vs kg/m^2 ≈ mm everywhere
    # else) — own product code so the m → in conversion can't collide
    # with precipTotal's mm → in.
    ecmwf_match: "^tp:sfc:"
    wgrib2_match: ""
    units_in: "m"
    units_out: "in"
    convert: "A * 39.3701"
    clr: "precip.clr"
    pal: "gfs_precipTotal.pal"
    gpu_data: { min: 0.0, max: 20.0 }
    point_values: true
    point_decimals: 2

  precip3h:
    display: "3-hour Precipitation"
    group: "Surface & Precipitation/Precipitation"
    # NAM's APCP buckets reset every 3 hours (f01 "0-1", f02 "0-2",
    # f03 "0-3", f04 "3-4", ...) — a constant-width bucket only completes
    # at 3-hour multiples, so fh_step renders every 3rd frame and the
    # manifest's expected list matches (see build_manifest.py). There is
    # no running-total APCP in the NAM nest files, hence no precipTotal.
    wgrib2_match: ":APCP:surface:{fh_minus_3}-{fh} hour acc"
    # Open-Meteo `precipitation` on a 3-hourly-step model IS the 3-h sum.
    om_var: "precipitation"
    fh_min: 3
    fh_step: 3
    units_in: "kg/m^2"
    units_out: "in"
    convert: "A / 25.4"
    clr: "precip.clr"
    pal: "hrrr_precip1h.pal"
    gpu_data: { min: 0.0, max: 8.0 }
    point_values: true
    point_decimals: 2

  precip6h:
    display: "6-hour Precipitation"
    group: "Surface & Precipitation/Precipitation"
    # GFS APCP buckets reset every 6 h — the constant-width 6-h bucket
    # only exists at 6-hour-multiple steps ("6-12" at f012), so fh_step 6.
    # Same pattern as NAM's precip3h; {fh_minus_6} substitution lives in
    # decode_pipeline.sh alongside _1 and _3.
    wgrib2_match: ":APCP:surface:{fh_minus_6}-{fh} hour acc"
    # Open-Meteo `precipitation` on a 6-hourly-step model IS the 6-h sum.
    om_var: "precipitation"
    fh_min: 6
    fh_step: 6
    units_in: "kg/m^2"
    units_out: "in"
    convert: "A / 25.4"
    clr: "precip.clr"
    pal: "hrrr_precip1h.pal"
    gpu_data: { min: 0.0, max: 8.0 }
    point_values: true
    point_decimals: 2

  # ── Pressure-level wind family ─────────────────────────────────────────
  # One entry per level (925 low-level jet .. 200 jet stream). All share
  # wind.clr + the gfs_wind500 legend; ECMWF matches included — its open
  # data publishes u/v at every one of these levels (verified 2026-08-06).

  wind925:
    display: "925mb Wind Speed"
    group: "Upper Air/Wind"
    wgrib2_match: ":(UGRD|VGRD):925 mb:"
    ecmwf_match: "^(u|v):pl:925$"
    composite_uv: true
    units_in: "m/s"
    units_out: "kt"
    convert: "A * 1.94384"
    clr: "wind.clr"
    pal: "gfs_wind500.pal"
    gpu_data: { min: 0.0, max: 200.0 }
    point_values: true

  wind850:
    display: "850mb Wind Speed"
    group: "Upper Air/Wind"
    wgrib2_match: ":(UGRD|VGRD):850 mb:"
    ecmwf_match: "^(u|v):pl:850$"
    om_uv: ["wind_u_component_850hPa", "wind_v_component_850hPa"]
    composite_uv: true
    units_in: "m/s"
    units_out: "kt"
    convert: "A * 1.94384"
    clr: "wind.clr"
    pal: "gfs_wind500.pal"
    gpu_data: { min: 0.0, max: 200.0 }
    point_values: true

  wind700:
    display: "700mb Wind Speed"
    group: "Upper Air/Wind"
    wgrib2_match: ":(UGRD|VGRD):700 mb:"
    ecmwf_match: "^(u|v):pl:700$"
    om_uv: ["wind_u_component_700hPa", "wind_v_component_700hPa"]
    composite_uv: true
    units_in: "m/s"
    units_out: "kt"
    convert: "A * 1.94384"
    clr: "wind.clr"
    pal: "gfs_wind500.pal"
    gpu_data: { min: 0.0, max: 200.0 }
    point_values: true

  wind300:
    display: "300mb Wind Speed"
    group: "Upper Air/Wind"
    wgrib2_match: ":(UGRD|VGRD):300 mb:"
    ecmwf_match: "^(u|v):pl:300$"
    om_uv: ["wind_u_component_300hPa", "wind_v_component_300hPa"]
    composite_uv: true
    units_in: "m/s"
    units_out: "kt"
    convert: "A * 1.94384"
    clr: "wind.clr"
    pal: "gfs_wind500.pal"
    gpu_data: { min: 0.0, max: 200.0 }
    point_values: true

  wind250:
    display: "250mb Wind Speed"
    group: "Upper Air/Wind"
    wgrib2_match: ":(UGRD|VGRD):250 mb:"
    ecmwf_match: "^(u|v):pl:250$"
    om_uv: ["wind_u_component_250hPa", "wind_v_component_250hPa"]
    composite_uv: true
    units_in: "m/s"
    units_out: "kt"
    convert: "A * 1.94384"
    clr: "wind.clr"
    pal: "gfs_wind500.pal"
    gpu_data: { min: 0.0, max: 200.0 }
    point_values: true

  wind200:
    display: "200mb Wind Speed"
    group: "Upper Air/Wind"
    wgrib2_match: ":(UGRD|VGRD):200 mb:"
    ecmwf_match: "^(u|v):pl:200$"
    om_uv: ["wind_u_component_200hPa", "wind_v_component_200hPa"]
    composite_uv: true
    units_in: "m/s"
    units_out: "kt"
    convert: "A * 1.94384"
    clr: "wind.clr"
    pal: "gfs_wind500.pal"
    gpu_data: { min: 0.0, max: 200.0 }
    point_values: true

  # ── Upper-air moisture (pressure-level RH) ─────────────────────────────

  rh925:
    display: "925mb Relative Humidity"
    group: "Upper Air/Moisture"
    wgrib2_match: ":RH:925 mb:"
    ecmwf_match: "^r:pl:925$"
    units_in: "%"
    units_out: "%"
    convert: ""
    clr: "uarh.clr"
    pal: ""
    interpolate_color: true
    point_values: true

  rh850:
    display: "850mb Relative Humidity"
    group: "Upper Air/Moisture"
    wgrib2_match: ":RH:850 mb:"
    ecmwf_match: "^r:pl:850$"
    om_var: "relative_humidity_850hPa"
    units_in: "%"
    units_out: "%"
    convert: ""
    clr: "uarh.clr"
    pal: ""
    interpolate_color: true
    point_values: true

  rh700:
    display: "700mb Relative Humidity"
    group: "Upper Air/Moisture"
    wgrib2_match: ":RH:700 mb:"
    ecmwf_match: "^r:pl:700$"
    om_var: "relative_humidity_700hPa"
    units_in: "%"
    units_out: "%"
    convert: ""
    clr: "uarh.clr"
    pal: ""
    interpolate_color: true
    point_values: true

  rh500:
    display: "500mb Relative Humidity"
    group: "Upper Air/Moisture"
    wgrib2_match: ":RH:500 mb:"
    ecmwf_match: "^r:pl:500$"
    om_var: "relative_humidity_500hPa"
    units_in: "%"
    units_out: "%"
    convert: ""
    clr: "uarh.clr"
    pal: ""
    interpolate_color: true
    point_values: true

  # ── Upper-air dynamics ─────────────────────────────────────────────────

  vort500:
    display: "500mb Absolute Vorticity"
    group: "Upper Air/Dynamics"
    wgrib2_match: ":ABSV:500 mb:"
    # Raw s^-1 -> display 10^-5 s^-1 (the classic chart unit).
    units_in: "1/s"
    units_out: "10⁻⁵/s"
    convert: "A * 100000.0"
    clr: "vort.clr"
    pal: ""
    interpolate_color: true
    point_values: true

  vv700:
    display: "700mb Vertical Velocity"
    group: "Upper Air/Dynamics"
    # ECMWF open data publishes omega as pl param 'w' (Pa/s, same sign
    # convention).
    ecmwf_match: "^w:pl:700$"
    wgrib2_match: ":VVEL:700 mb:"
    # Omega in Pa/s; NEGATIVE = lift. vv.clr paints lift warm.
    units_in: "Pa/s"
    units_out: "Pa/s"
    convert: ""
    clr: "vv.clr"
    pal: ""
    interpolate_color: true
    point_values: true
    point_decimals: 1

  vv850:
    display: "850mb Vertical Velocity"
    group: "Upper Air/Dynamics"
    # GEFS's pgrb2a subset carries VVEL at 850 mb only (vv700 is the
    # full-res models' level).
    ecmwf_match: "^w:pl:850$"
    wgrib2_match: ":VVEL:850 mb:"
    units_in: "Pa/s"
    units_out: "Pa/s"
    convert: ""
    clr: "vv.clr"
    pal: ""
    interpolate_color: true
    point_values: true
    point_decimals: 1

  capeML180:
    display: "CAPE (180-0mb)"
    group: "Severe Weather/Instability"
    # GEFS pgrb2a's only CAPE layer — a deep mixed-layer variant, own
    # code so it never masquerades as surface-based or 90-0 ML CAPE.
    wgrib2_match: ":CAPE:180-0 mb above ground:"
    units_in: "J/kg"
    units_out: "J/kg"
    convert: ""
    clr: "cape.clr"
    pal: ""
    point_values: true

  cinML180:
    display: "CIN (180-0mb)"
    group: "Severe Weather/Instability"
    wgrib2_match: ":CIN:180-0 mb above ground:"
    units_in: "J/kg"
    units_out: "J/kg"
    convert: ""
    clr: "cin.clr"
    pal: ""
    point_values: true

  shearSfc500:
    display: "Bulk Shear (Sfc-500mb)"
    group: "Severe Weather/Wind Shear"
    derived: true
    # |V(500mb) - V(10m)| in kt — Pivotal's "Bulk Shear: Surface-500 mb".
    # GFS carries no VUCSH fields, so this stands in for deep-layer shear.
    inputs:
      A: ":UGRD:10 m above ground:"
      B: ":VGRD:10 m above ground:"
      C: ":UGRD:500 mb:"
      D: ":VGRD:500 mb:"
    calc: "sqrt((C-A)*(C-A)+(D-B)*(D-B))*1.94384"
    units_in: ""
    units_out: "kt"
    convert: ""
    clr: "shear.clr"
    pal: ""
    point_values: true

  wind500:
    display: "500mb Wind Speed"
    group: "Upper Air/Wind"
    wgrib2_match: ":(UGRD|VGRD):500 mb:"
    ecmwf_match: "^(u|v):pl:500$"
    om_uv: ["wind_u_component_500hPa", "wind_v_component_500hPa"]
    composite_uv: true
    units_in: "m/s"
    units_out: "kt"
    convert: "A * 1.94384"
    clr: "wind.clr"
    pal: "gfs_wind500.pal"
    gpu_data: { min: 0.0, max: 200.0 }
    point_values: true

  precipTotal:
    display: "Total Precipitation"
    group: "Surface & Precipitation/Precipitation"
    # fh_min: the f00 running total is zero by definition — GFS doesn't
    # even publish an f000 APCP message (its f00 slot painted "missing"
    # red on the scrub bar since launch; this fixes that), and RRFS
    # lacks it too. Added 2026-08-05 with the NAM/RRFS expansion.
    fh_min: 1
    wgrib2_match: ":APCP:surface:0-"
    units_in: "kg/m^2"
    units_out: "in"
    convert: "A / 25.4"
    clr: "precip.clr"
    pal: "gfs_precipTotal.pal"
    gpu_data: { min: 0.0, max: 20.0 }
    point_values: true
    point_decimals: 2

  # ── Cloud cover family (added 2026-08-05, Pivotal parity) ──────────────
  # HRRR wrfsfc carries all four at every hour incl. f00 (verified against
  # the 2026-08-05 18z idx). ⚠ TCDC appears TWICE in the file — "entire
  # atmosphere" AND "boundary layer cloud layer" — so the match anchors the
  # level text. No-trailing-colon prefix like refc, so an RRFS-style
  # "(considered as a single layer)" suffix still matches if these are ever
  # mirrored there. interpolate_color: coverage is a continuous veil, not a
  # threshold-read parameter — hard bins would just look like banding.

  tcc:
    display: "Cloud Cover"
    group: "Clouds & Satellite"
    wgrib2_match: ":TCDC:entire atmosphere"
    om_var: "cloud_cover"
    units_in: "%"
    units_out: "%"
    convert: ""
    clr: "cloud.clr"
    pal: ""
    interpolate_color: true
    point_values: true

  lcc:
    display: "Cloud Cover (Low)"
    group: "Clouds & Satellite"
    wgrib2_match: ":LCDC:low cloud layer:"
    om_var: "cloud_cover_low"
    units_in: "%"
    units_out: "%"
    convert: ""
    clr: "cloudlow.clr"
    pal: ""
    interpolate_color: true
    point_values: true

  mcc:
    display: "Cloud Cover (Mid)"
    group: "Clouds & Satellite"
    wgrib2_match: ":MCDC:middle cloud layer:"
    om_var: "cloud_cover_mid"
    units_in: "%"
    units_out: "%"
    convert: ""
    clr: "cloudmid.clr"
    pal: ""
    interpolate_color: true
    point_values: true

  hcc:
    display: "Cloud Cover (High)"
    group: "Clouds & Satellite"
    wgrib2_match: ":HCDC:high cloud layer:"
    om_var: "cloud_cover_high"
    units_in: "%"
    units_out: "%"
    convert: ""
    clr: "cloudhigh.clr"
    pal: ""
    interpolate_color: true
    point_values: true

  # ── Pivotal-parity batch 2 (added 2026-08-05) ───────────────────────────
  # Ten more single-message wrfsfc fields, all verified in the 2026-08-05
  # 18z idx at f00 AND f09. Accumulation/max fields carry "0-0 day acc/max"
  # text at f00 vs "0-N hour" later, so their matches anchor on the level
  # text (or the "0-" accumulation prefix) and deliberately stop there.

  refd1km:
    display: "1km AGL Reflectivity"
    group: "Surface & Precipitation/Radar"
    # File also carries REFD at 4000 m and 263 K — anchor the level.
    wgrib2_match: ":REFD:1000 m above ground:"
    units_in: "dBZ"
    units_out: "dBZ"
    convert: ""
    clr: "refc.clr"
    pal: "hrrr_refc.pal"
    # Same smooth gradient + palette as composite reflectivity.
    interpolate_color: true

  irsat:
    display: "Simulated IR Satellite"
    group: "Clouds & Satellite"
    # SBT124 = simulated GOES-East longwave-IR-window brightness temp.
    # (SBT123 in the same file is the water-vapor channel — a possible
    # future product, don't loosen this match.)
    wgrib2_match: ":SBT124:top of atmosphere:"
    # ⚠ Unlike t2m, GDAL does NOT K->C normalize SBT brightness temps —
    # verified 2026-08-05 when the Celsius-keyed first attempt rendered
    # single-color. irsat.clr is keyed in raw KELVIN.
    units_in: "K"
    units_out: "K"
    convert: ""
    clr: "irsat.clr"
    pal: ""
    interpolate_color: true

  smokesfc:
    display: "Near-Surface Smoke"
    group: "Fire Weather"
    wgrib2_match: ":MASSDEN:8 m above ground:"
    # Raw kg/m^3 -> ug/m^3. ⚠ verify against the first render's logged
    # raw.tif stats (expected raw magnitudes ~1e-9..1e-7).
    units_in: "kg/m^3"
    units_out: "µg/m³"
    convert: "A * 1000000000.0"
    clr: "smoke.clr"
    pal: ""
    interpolate_color: true
    point_values: true

  smokeint:
    display: "Integrated Smoke"
    group: "Fire Weather"
    wgrib2_match: ":COLMD:entire atmosphere"
    # Raw kg/m^2 -> mg/m^2. Same first-render stats check as smokesfc.
    units_in: "kg/m^2"
    units_out: "mg/m²"
    convert: "A * 1000000.0"
    clr: "smokeint.clr"
    pal: ""
    interpolate_color: true
    point_values: true

  ltng:
    display: "Lightning Threat"
    group: "Severe Weather/Composites"
    # fh_min: HRRR carries an f00 'anl' message but NAM/RRFS only publish
    # LTNG on forecast hours (RRFS as an hourly max) — and the f00 threat
    # is zero anyway, so skipping f00 everywhere keeps the shared entry
    # consistent across models.
    fh_min: 1
    # Level text differs by model: HRRR/RRFS say "entire atmosphere",
    # NAM writes ":LTNG:surface:" (found 2026-08-05 when NAM rendered 0
    # ltng frames while its whole hazards group succeeded).
    wgrib2_match: ":LTNG:(entire atmosphere|surface):"
    units_in: "flashes/km^2"
    units_out: "fl/km²"
    convert: ""
    clr: "ltng.clr"
    pal: ""
    point_values: true
    point_decimals: 1

  sbcin:
    display: "Surface CIN"
    group: "Severe Weather/Instability"
    # File carries CIN at surface, 180-0, 90-0 (mlcin) and 255-0 mb —
    # ":CIN:surface:" is unique.
    wgrib2_match: ":CIN:surface:"
    # Open-Meteo publishes CIN positive-magnitude — flip to the negative
    # NOAA convention cin.clr keys on (same gotcha as cinML).
    om_var: "convective_inhibition"
    om_convert: "A * -1.0"
    units_in: "J/kg"
    units_out: "J/kg"
    convert: ""
    clr: "cin.clr"
    pal: ""
    point_values: true

  # ── Open-Meteo-sourced severe products (ICOND2 pilot) ──────────────────
  # DWD ICON's CAPE/CIN are MIXED-LAYER parcels (CAPE_ML/CIN_ML), so they
  # get honest codes instead of riding sfccape/sbcin's "Surface" labels.
  capeML:
    display: "CAPE (Mixed Layer)"
    group: "Severe Weather/Instability"
    wgrib2_match: ""
    om_var: "cape"
    units_in: "J/kg"
    units_out: "J/kg"
    convert: ""
    clr: "cape.clr"
    pal: ""
    gpu_data: { min: 0.0, max: 6000.0 }
    point_values: true
  cinML:
    display: "CIN (Mixed Layer)"
    group: "Severe Weather/Instability"
    wgrib2_match: ""
    # Open-Meteo publishes CIN as a POSITIVE magnitude (verified max ~660
    # on 2026-08-13); flip to the negative NOAA convention cin.clr keys on.
    om_var: "convective_inhibition"
    om_convert: "A * -1.0"
    units_in: "J/kg"
    units_out: "J/kg"
    convert: ""
    clr: "cin.clr"
    pal: ""
    point_values: true
  lpi:
    display: "Lightning Potential"
    group: "Severe Weather/Composites"
    wgrib2_match: ""
    # ICON's LPI (Lightning Potential Index, J/kg) — a different quantity
    # than HRRR's LTNG flash-rate product, so it keeps its own code/ramp.
    om_var: "lightning_potential"
    units_in: "J/kg"
    units_out: "J/kg"
    convert: ""
    clr: "lpi.clr"
    pal: ""
    point_values: true
    point_decimals: 1
  updraft:
    display: "Max Updraft"
    group: "Severe Weather/Composites"
    wgrib2_match: ""
    # ICON max updraft velocity in the column (m/s) — the closest thing to
    # a storm-intensity proxy the om set offers with no reflectivity field.
    # Max-over-past-hour field: absent from f00 analysis files (82-var set),
    # hence the om_fh_min gate (mirrored into expected hours by
    # build_manifest.py so the scrub bar doesn't paint f00 red).
    om_var: "updraft"
    om_fh_min: 1
    units_in: "m/s"
    units_out: "m/s"
    convert: ""
    clr: "updraft.clr"
    pal: ""
    point_values: true
    point_decimals: 1

  uh25:
    display: "Updraft Helicity (1h max)"
    group: "Severe Weather/Wind Shear"
    # Hourly-max 2-5 km UH. File also has 2000-0 and 3000-0 m layers.
    # fh_min: NAM/RRFS have no f00 MXUPHL message (HRRR's is a zeros
    # '0-0 day max') — skip f00 on every model for consistency.
    fh_min: 1
    wgrib2_match: ":MXUPHL:5000-2000 m above ground:"
    units_in: "m^2/s^2"
    units_out: "m²/s²"
    convert: ""
    clr: "uh.clr"
    pal: ""
    point_values: true

  snod:
    display: "Snow Depth"
    group: "Winter Weather"
    wgrib2_match: ":SNOD:surface:"
    om_var: "snow_depth"
    units_in: "m"
    units_out: "in"
    convert: "A * 39.3701"
    clr: "snow.clr"
    pal: ""
    point_values: true
    point_decimals: 1

  asnow:
    display: "Total Snowfall"
    group: "Winter Weather"
    # HRRR's native ASNOW accumulation (what Pivotal labels Total
    # Snowfall) — NOT a Kuchera-ratio product; that needs the wrfprs
    # column temps and stays out of scope.
    # fh_min: no f00 message on RRFS (HRRR's is a zeros '0-0 day acc').
    fh_min: 1
    wgrib2_match: ":ASNOW:surface:0-"
    units_in: "m"
    units_out: "in"
    convert: "A * 39.3701"
    clr: "snow.clr"
    pal: ""
    point_values: true
    point_decimals: 1

  frzr:
    display: "Total Freezing Rain"
    group: "Winter Weather"
    # fh_min: no f00 running-total message on RRFS; HRRR's is zeros.
    fh_min: 1
    wgrib2_match: ":FRZR:surface:0-"
    units_in: "kg/m^2"
    units_out: "in"
    convert: "A / 25.4"
    clr: "ice.clr"
    pal: ""
    point_values: true
    point_decimals: 2

  # ── Mesoanalysis products ────────────────────────────────────────────────
  # Severe-weather parameters for the app's Mesoanalysis map layer
  # (SPC-mesoanalysis-style shaded fields under radar).
  #
  # HISTORY: this whole section began as analysis-only (fh_cap: 1 +
  # category: meso — f00/f01 frames, mesoCatalog-only so the models tab
  # never listed them). srh1/srh3/ehi1/ehi3 were promoted to full-forecast
  # `also_meso` products on 2026-07-27, and the REST of the section followed
  # on 2026-08-05 (Pivotal-parity request) once the render workflow fanned
  # out into parallel product-group jobs and could afford the frames. Every
  # product here is now dual-role: scrubbable in the models tab AND listed
  # in the meso picker (whose layer just consumes the f00 frame these
  # renders already produce).
  #
  # For any FUTURE analysis-only product, the old mechanics still work:
  #   fh_cap: 1       render only f00 + f01 (bridge frame while the next
  #                   run's f00 is still publishing)
  #   category: meso  publish in mesoCatalog ONLY, so app versions that
  #                   predate the meso layer never see a 2-frame product
  #                   in the forecast-scrub models picker
  #
  # Derived products (stp, scp) set `derived: true` with named `inputs`
  # (letter -> idx match regex, one message each) and a gdal_calc `calc`
  # expression over those letters. Formulas are kept EXACTLY in sync with
  # the app's sounding_analysis.dart (_stpFixed / _scp) so a tapped map
  # value never disagrees with the sounding sheet at the same point:
  #   * STP includes Thompson's CIN term (the app's deliberate choice) —
  #     values run lower than SPC's fixed-layer STP wherever SBCIN < -50.
  #   * SCP uses SRH3/BWD6 (the app's fallback when effective-layer values
  #     are unavailable).

  mlcape:
    display: "MLCAPE"
    group: "Severe Weather/Instability"
    wgrib2_match: ":CAPE:90-0 mb above ground:"
    units_in: "J/kg"
    units_out: "J/kg"
    convert: ""
    clr: "cape.clr"
    pal: ""
    point_values: true
    also_meso: true
    # Denser value grid than the model default (128): the cell-picker
    # readout interpolates this, and 512 =~ 12 km/sample over CONUS.
    point_grid: [512, 512]

  mucape:
    display: "MUCAPE"
    group: "Severe Weather/Instability"
    wgrib2_match: ":CAPE:255-0 mb above ground:"
    units_in: "J/kg"
    units_out: "J/kg"
    convert: ""
    clr: "cape.clr"
    pal: ""
    point_values: true
    also_meso: true
    # Denser value grid than the model default (128): the cell-picker
    # readout interpolates this, and 512 =~ 12 km/sample over CONUS.
    point_grid: [512, 512]

  mlcin:
    display: "MLCIN"
    group: "Severe Weather/Instability"
    wgrib2_match: ":CIN:90-0 mb above ground:"
    units_in: "J/kg"
    units_out: "J/kg"
    convert: ""
    clr: "cin.clr"
    pal: ""
    point_values: true
    also_meso: true
    # Denser value grid than the model default (128): the cell-picker
    # readout interpolates this, and 512 =~ 12 km/sample over CONUS.
    point_grid: [512, 512]

  # Full-range 512² point grids (no point_value_fh_cap) on this whole
  # section: the crosshair / long-press inspector samples these at whatever
  # hour the models tab has scrubbed to, so every frame needs its JSON.
  # srh/ehi briefly shipped capped at f00/f01 (2026-07-27, same day) before
  # the inspector work made that a dead end. ~930 KB/frame raw but
  # ~50-200 KB over the wire (CDN gzip) — pennies on B2, uploads free.

  srh1:
    display: "0-1km SRH"
    group: "Severe Weather/Wind Shear"
    wgrib2_match: ":HLCY:1000-0 m above ground:"
    units_in: "m^2/s^2"
    units_out: "m²/s²"
    convert: ""
    clr: "srh.clr"
    pal: ""
    point_values: true
    also_meso: true
    # Denser value grid than the model default (128): the cell-picker
    # readout interpolates this, and 512 =~ 12 km/sample over CONUS.
    point_grid: [512, 512]

  srh3:
    display: "0-3km SRH"
    group: "Severe Weather/Wind Shear"
    wgrib2_match: ":HLCY:3000-0 m above ground:"
    units_in: "m^2/s^2"
    units_out: "m²/s²"
    convert: ""
    clr: "srh.clr"
    pal: ""
    point_values: true
    also_meso: true
    # Denser value grid than the model default (128): the cell-picker
    # readout interpolates this, and 512 =~ 12 km/sample over CONUS.
    point_grid: [512, 512]

  shear6:
    display: "0-6km Shear"
    group: "Severe Weather/Wind Shear"
    # ⚠ HRRR labels VUCSH/VVCSH as [1/s] but the encoded values are the
    # bulk wind-difference vector components in m/s (verified empirically
    # 2026-07-08: magnitudes 0-35 with p99 ≈ 23 — true shear-per-second
    # would be ~0.005). Treat exactly like a U/V wind pair.
    wgrib2_match: ":(VUCSH|VVCSH):0-6000 m above ground:"
    composite_uv: true
    units_in: "m/s"
    units_out: "kt"
    convert: "A * 1.94384"
    clr: "shear.clr"
    pal: ""
    point_values: true
    also_meso: true
    # Denser value grid than the model default (128): the cell-picker
    # readout interpolates this, and 512 =~ 12 km/sample over CONUS.
    point_grid: [512, 512]

  stp:
    display: "Sig. Tornado (STP)"
    group: "Severe Weather/Composites"
    derived: true
    # Mirrors sounding_analysis.dart _stpFixed (STP-cin variant):
    #   capeTerm  = SBCAPE/1500
    #   lclTerm   = clip((2000 - LCL_agl)/1000, 0, 1)   LCL_agl = LCL_msl - terrain
    #   srhTerm   = SRH1/150
    #   shearTerm = 0 below 12.5 m/s, else min(shr6,30)/20  (cap 1.5)
    #   cinTerm   = clip((200 + SBCIN)/150, 0, 1)        (SBCIN is negative)
    inputs:
      A: ":CAPE:surface:"
      B: ":CIN:surface:"
      C: ":HGT:level of adiabatic condensation from sfc:"
      D: ":HGT:surface:"
      E: ":HLCY:1000-0 m above ground:"
      F: ":VUCSH:0-6000 m above ground:"
      G: ":VVCSH:0-6000 m above ground:"
    calc: "maximum((A/1500.0)*clip((2000.0-(C-D))/1000.0,0,1)*(E/150.0)*where(sqrt(F*F+G*G)<12.5,0,minimum(sqrt(F*F+G*G),30.0)/20.0)*clip((200.0+B)/150.0,0,1),0)"
    units_in: ""
    units_out: ""
    convert: ""
    clr: "stp.clr"
    pal: ""
    point_values: true
    point_decimals: 1
    also_meso: true
    # Denser value grid than the model default (128): the cell-picker
    # readout interpolates this, and 512 =~ 12 km/sample over CONUS.
    point_grid: [512, 512]

  scp:
    display: "Supercell Comp. (SCP)"
    group: "Severe Weather/Composites"
    derived: true
    # Mirrors sounding_analysis.dart _scp with the SRH3/BWD6 fallback:
    #   (MUCAPE/1000) * (SRH3/50) * shearTerm
    #   shearTerm = 0 below 10 m/s, else min(shr6,20)/20  (cap 1.0)
    inputs:
      A: ":CAPE:255-0 mb above ground:"
      B: ":HLCY:3000-0 m above ground:"
      C: ":VUCSH:0-6000 m above ground:"
      D: ":VVCSH:0-6000 m above ground:"
    calc: "maximum((A/1000.0)*(B/50.0)*where(sqrt(C*C+D*D)<10.0,0,minimum(sqrt(C*C+D*D),20.0)/20.0),0)"
    units_in: ""
    units_out: ""
    convert: ""
    clr: "scp.clr"
    pal: ""
    point_values: true
    point_decimals: 1
    also_meso: true
    # Denser value grid than the model default (128): the cell-picker
    # readout interpolates this, and 512 =~ 12 km/sample over CONUS.
    point_grid: [512, 512]

  # ── Mesoanalysis catalog v2 (SPC-style groups) ──────────────────────────
  # Same rules as the first meso set: fh_cap 1, category meso, formulas
  # kept dumb enough to audit against the SPC page. Grouping lives in the
  # app's kMesoParameters (Thermodynamics / Wind Shear / Composite Indices /
  # Surface / Heavy Rain / Winter Weather / Fire Weather).

  cape3km:
    display: "0-3km CAPE"
    group: "Severe Weather/Instability"
    wgrib2_match: ":CAPE:0-3000 m above ground:"
    units_in: "J/kg"
    units_out: "J/kg"
    convert: ""
    clr: "cape3km.clr"
    pal: ""
    point_values: true
    also_meso: true
    # Denser value grid than the model default (128): the cell-picker
    # readout interpolates this, and 512 =~ 12 km/sample over CONUS.
    point_grid: [512, 512]

  liftedIdx:
    display: "Lifted Index"
    group: "Severe Weather/Instability"
    # ⚠ LFTX is a temperature-typed field (K): if GDAL's CF normalization
    # subtracts 273.15 the raw stats will sit near -280 — check the logged
    # raw.tif stats on first render and add `convert: "A + 273.15"` if so.
    wgrib2_match: ":LFTX:500-1000 mb:"
    units_in: "K"
    units_out: "°C"
    convert: ""
    clr: "lifted.clr"
    pal: ""
    point_values: true
    also_meso: true
    # Denser value grid than the model default (128): the cell-picker
    # readout interpolates this, and 512 =~ 12 km/sample over CONUS.
    point_grid: [512, 512]

  lapse75:
    display: "700-500mb Lapse Rate"
    group: "Severe Weather/Instability"
    derived: true
    # (T700 - T500) / (z500 - z700) in °C/km. Temperature DIFFERENCE, so
    # any K→C normalization by the GRIB driver cancels out. max() guards
    # a degenerate thickness (never happens on real data).
    inputs:
      A: ":TMP:700 mb:"
      B: ":TMP:500 mb:"
      C: ":HGT:500 mb:"
      D: ":HGT:700 mb:"
    calc: "(A-B)/maximum((C-D)/1000.0,0.1)"
    units_in: ""
    units_out: "°C/km"
    convert: ""
    clr: "lapse.clr"
    pal: ""
    point_values: true
    point_decimals: 1
    also_meso: true
    # Denser value grid than the model default (128): the cell-picker
    # readout interpolates this, and 512 =~ 12 km/sample over CONUS.
    point_grid: [512, 512]

  lclHgt:
    display: "LCL Height"
    group: "Severe Weather/Instability"
    derived: true
    # LCL above ground = LCL MSL height minus terrain height.
    inputs:
      A: ":HGT:level of adiabatic condensation from sfc:"
      B: ":HGT:surface:"
    calc: "maximum(A-B,0)"
    units_in: "m"
    units_out: "m"
    convert: ""
    clr: "lcl.clr"
    pal: ""
    point_values: true
    also_meso: true
    # Denser value grid than the model default (128): the cell-picker
    # readout interpolates this, and 512 =~ 12 km/sample over CONUS.
    point_grid: [512, 512]

  shear1:
    display: "0-1km Shear"
    group: "Severe Weather/Wind Shear"
    # Same [1/s]-labeled-but-actually-m/s gotcha as shear6.
    wgrib2_match: ":(VUCSH|VVCSH):0-1000 m above ground:"
    composite_uv: true
    units_in: "m/s"
    units_out: "kt"
    convert: "A * 1.94384"
    clr: "shear1.clr"
    pal: ""
    point_values: true
    also_meso: true
    # Denser value grid than the model default (128): the cell-picker
    # readout interpolates this, and 512 =~ 12 km/sample over CONUS.
    point_grid: [512, 512]

  stormMotion:
    display: "Storm Motion"
    group: "Severe Weather/Wind Shear"
    # Bunkers right-mover storm motion vector -> speed. (Direction can't
    # render as a raster; the sounding hodograph covers it.)
    # Level text differs by model: HRRR writes "0-6000 m", NAM/RRFS write
    # "6000-0 m" for the SAME layer — the alternation covers both.
    wgrib2_match: ":(USTM|VSTM):(0-6000|6000-0) m above ground:"
    composite_uv: true
    units_in: "m/s"
    units_out: "kt"
    convert: "A * 1.94384"
    clr: "wind.clr"
    pal: ""
    point_values: true
    also_meso: true
    # Denser value grid than the model default (128): the cell-picker
    # readout interpolates this, and 512 =~ 12 km/sample over CONUS.
    point_grid: [512, 512]

  # ehi1/ehi3: dual-role like srh1/srh3 — see the note above those entries.
  # Both mirror sounding_analysis.dart _ehi exactly, (CAPE * SRH) / 160000
  # floored at 0, with the SAME surface-based CAPE the sounding sheet feeds
  # it (_ehi(sb.capeJkg, srh1) / _ehi(sb.capeJkg, srh3)). SPC's mesoanalysis
  # page uses MLCAPE for EHI, so these run a bit hotter than SPC's under a
  # capped/heated boundary layer — that's the deliberate app-agreement rule
  # already applied to STP/SCP. Do NOT "fix" one side alone.
  ehi1:
    display: "EHI (0-1km)"
    group: "Severe Weather/Composites"
    derived: true
    inputs:
      A: ":CAPE:surface:"
      B: ":HLCY:1000-0 m above ground:"
    calc: "maximum((A*B)/160000.0,0)"
    units_in: ""
    units_out: ""
    convert: ""
    clr: "ehi.clr"
    pal: ""
    point_values: true
    point_decimals: 1
    also_meso: true
    # Denser value grid than the model default (128): the cell-picker
    # readout interpolates this, and 512 =~ 12 km/sample over CONUS.
    point_grid: [512, 512]

  ehi3:
    display: "EHI (0-3km)"
    group: "Severe Weather/Composites"
    derived: true
    inputs:
      A: ":CAPE:surface:"
      B: ":HLCY:3000-0 m above ground:"
    calc: "maximum((A*B)/160000.0,0)"
    units_in: ""
    units_out: ""
    convert: ""
    # Shares ehi.clr with ehi1. 0-3 km EHI runs ~2-3x the 0-1 km value for
    # the same profile (SRH3 >> SRH1), so the ramp's upper bins do the work
    # here — matching the app, whose ehi3 legend reuses the ehi1 stops.
    clr: "ehi.clr"
    pal: ""
    point_values: true
    point_decimals: 1
    also_meso: true
    # Denser value grid than the model default (128): the cell-picker
    # readout interpolates this, and 512 =~ 12 km/sample over CONUS.
    point_grid: [512, 512]

  pwat:
    display: "Precipitable Water"
    group: "Surface & Precipitation/Precipitation"
    # ECMWF's tcwv (total column water vapour) is the same quantity in
    # the same kg/m^2, so the shared mm -> in convert applies.
    ecmwf_match: "^tcwv:sfc:"
    wgrib2_match: ":PWAT:entire atmosphere"
    units_in: "kg/m^2"
    units_out: "in"
    convert: "A / 25.4"
    clr: "pwat.clr"
    pal: ""
    point_values: true
    point_decimals: 2
    also_meso: true
    # Denser value grid than the model default (128): the cell-picker
    # readout interpolates this, and 512 =~ 12 km/sample over CONUS.
    point_grid: [512, 512]

  echoTops:
    display: "Echo Tops"
    group: "Severe Weather/Composites"
    # Level descriptor differs per model (HRRR ":RETOP:cloud top:", RRFS
    # ":RETOP:entire atmosphere (considered as a single layer):" — found
    # 2026-08-13; RRFS echoTops had silently never rendered). Both files
    # carry exactly one RETOP message, so the bare var match is safe.
    wgrib2_match: ":RETOP:"
    units_in: "m"
    units_out: "kft"
    convert: "A * 0.00328084"
    clr: "etops.clr"
    pal: ""
    point_values: true
    also_meso: true
    # Denser value grid than the model default (128): the cell-picker
    # readout interpolates this, and 512 =~ 12 km/sample over CONUS.
    point_grid: [512, 512]

  ptype:
    display: "Precip Type"
    group: "Surface & Precipitation/Precipitation"
    derived: true
    # Categorical composite of HRRR's dominant-type 0/1 flags:
    # 1 rain, 2 snow, 3 sleet, 4 freezing rain. Rendered with the
    # default nearest-color mapping (discrete bins); no point values —
    # the numbers would be category codes, not measurements.
    #
    # fh_min: the categorical flags are ALL-ZERO at f00 (verified
    # 2026-07-08: f00 CRAIN nonzero=0, f01 nonzero=109k) — HRRR only
    # populates them on forecast hours. Render starts at f01; the app's
    # meso layer follows each product's first expected hour.
    fh_min: 1
    inputs:
      A: ":CRAIN:surface:"
      B: ":CSNOW:surface:"
      C: ":CICEP:surface:"
      D: ":CFRZR:surface:"
    calc: "A*1+B*2+C*3+D*4"
    units_in: ""
    units_out: ""
    convert: ""
    clr: "ptype.clr"
    pal: ""
    point_values: false
    also_meso: true
    # Denser value grid than the model default (128): the cell-picker
    # readout interpolates this, and 512 =~ 12 km/sample over CONUS.
    point_grid: [512, 512]

  frzLvl:
    display: "Freezing Level"
    group: "Winter Weather"
    wgrib2_match: ":HGT:0C isotherm:"
    om_var: "freezing_level_height"
    units_in: "m"
    units_out: "m"
    convert: ""
    clr: "frzlvl.clr"
    pal: ""
    point_values: true
    also_meso: true
    # Denser value grid than the model default (128): the cell-picker
    # readout interpolates this, and 512 =~ 12 km/sample over CONUS.
    point_grid: [512, 512]

  rh2m:
    display: "2m Relative Humidity"
    group: "Surface & Precipitation/Surface"
    wgrib2_match: ":RH:2 m above ground:"
    om_var: "relative_humidity_2m"
    units_in: "%"
    units_out: "%"
    convert: ""
    clr: "rh.clr"
    pal: ""
    point_values: true
    also_meso: true
    # Denser value grid than the model default (128): the cell-picker
    # readout interpolates this, and 512 =~ 12 km/sample over CONUS.
    point_grid: [512, 512]

  vis:
    display: "Surface Visibility"
    group: "Surface & Precipitation/Surface"
    wgrib2_match: ":VIS:surface:"
    om_var: "visibility"
    units_in: "m"
    units_out: "mi"
    convert: "A / 1609.34"
    clr: "vis.clr"
    pal: ""
    point_values: true
    point_decimals: 1
    also_meso: true
    # Denser value grid than the model default (128): the cell-picker
    # readout interpolates this, and 512 =~ 12 km/sample over CONUS.
    point_grid: [512, 512]

  # ── RTMA surface observations (render_rtma_obs.sh) ─────────────────────
  # 2.5 km analysis-of-observations grids for the app's Observations layer
  # "Surface" section. The RTMA model block (bottom of this file) reuses the
  # shared t2m/td2m/wind10m/gust10m entries above — render_rtma_obs.sh does
  # its own field selection/conversion, reading only clr + gpu_data from
  # here, so wgrib2_match/ecmwf_match on the shared entries are ignored.
  # thetae is the one RTMA-only product.

  thetae:
    display: "2m Theta-E"
    # Derived in render_rtma_obs.sh (Bolton 1980) from TMP 2m + DPT 2m +
    # PRES surface — no wgrib2_match. Kelvin, SPC-mesoanalysis convention.
    units_in: "K"
    units_out: "K"
    convert: ""
    clr: "thetae.clr"
    pal: ""
    gpu_data: { min: 250.0, max: 400.0 }
    point_values: false
    group: "Surface"

  # ── Observed precipitation (MRMS QPE) ───────────────────────────────────
  # Radar+gauge observed accumulation for the app's "Observations (Precip)"
  # map layer. NOT rendered by decode_pipeline.sh — these have no runs or
  # forecast hours; scripts/render_mrms_qpe.sh fetches the MRMS grib2
  # directly from the noaa-mrms-pds AWS bucket and publishes each valid
  # hour as a single-frame pseudo-run (RunStamp = accumulation END hour,
  # frame F000) so build_manifest.py / prune_old_runs.py / the app's
  # frame-fetch plumbing all work unchanged.
  #
  #   mrms_product: source directory prefix on noaa-mrms-pds (the script
  #                 appends _Pass2_00.00 / _Pass1_00.00).
  # MRMS QPE encodes mm with sentinel negatives (-1 missing, -3 outside
  # radar coverage) — the render script maps A<0 → NoData before mm→in.

  qpe01h:
    display: "1-hour Precip"
    mrms_product: "MultiSensor_QPE_01H"
    units_in: "mm"
    units_out: "in"
    clr: "qpe.clr"
    # Additive value PNG (F000.data.png) for the app's crosshair inspector.
    # The colorized F000.png still renders the fill; this just gives the
    # readout the fill's own full-res values instead of the coarse 512²
    # point grid. max = qpe.clr ceiling (20 in) so it never clips below a
    # color the fill can show. See render_mrms_qpe.sh.
    gpu_data: { min: 0.0, max: 20.0 }
    pal: ""
    group: "Precipitation"
    point_values: true
    point_decimals: 2
    point_grid: [512, 512]

  qpe03h:
    display: "3-hour Precip"
    mrms_product: "MultiSensor_QPE_03H"
    units_in: "mm"
    units_out: "in"
    clr: "qpe.clr"
    gpu_data: { min: 0.0, max: 20.0 }
    pal: ""
    group: "Precipitation"
    point_values: true
    point_decimals: 2
    point_grid: [512, 512]

  qpe06h:
    display: "6-hour Precip"
    mrms_product: "MultiSensor_QPE_06H"
    units_in: "mm"
    units_out: "in"
    clr: "qpe.clr"
    gpu_data: { min: 0.0, max: 20.0 }
    pal: ""
    group: "Precipitation"
    point_values: true
    point_decimals: 2
    point_grid: [512, 512]

  qpe12h:
    display: "12-hour Precip"
    mrms_product: "MultiSensor_QPE_12H"
    units_in: "mm"
    units_out: "in"
    clr: "qpe.clr"
    gpu_data: { min: 0.0, max: 20.0 }
    pal: ""
    group: "Precipitation"
    point_values: true
    point_decimals: 2
    point_grid: [512, 512]

  qpe24h:
    display: "24-hour Precip"
    mrms_product: "MultiSensor_QPE_24H"
    units_in: "mm"
    units_out: "in"
    clr: "qpe.clr"
    gpu_data: { min: 0.0, max: 20.0 }
    pal: ""
    group: "Precipitation"
    point_values: true
    point_decimals: 2
    point_grid: [512, 512]

  # ── MRMS observation catalog (render_mrms_obs.sh) ──────────────────
  # Latest-frame products: sub-hourly publishes (2-min radar mosaics,
  # 10-min FLASH) with arbitrary -HHMMSS stamps and NO Pass1/Pass2 cycle,
  # so these use `mrms_dir` (the full CONUS/ prefix) instead of
  # `mrms_product`, and render_mrms_obs.sh lists the S3 day dir and takes
  # the newest object. `scale` multiplies values AFTER the A<0 sentinel
  # mask (-1 missing / -3 no-coverage → NoData); products left at 1 pass
  # native units through. `group` reaches the app via the manifest
  # catalog to build the categorized picker.

  # Hail
  mesh:
    display: "Hail Size (MESH)"
    mrms_dir: "MESH_00.50"
    data_png: { min: 0.0, max: 5.0 }
    units_in: "mm"
    units_out: "in"
    scale: 0.0393701
    clr: "mesh.clr"
    pal: ""
    group: "Hail"
    point_values: true
    point_decimals: 2
    point_grid: [512, 512]

  mesh60:
    display: "Hail Swath 1-hr"
    mrms_dir: "MESH_Max_60min_00.50"
    data_png: { min: 0.0, max: 5.0 }
    units_in: "mm"
    units_out: "in"
    scale: 0.0393701
    clr: "mesh.clr"
    pal: ""
    group: "Hail"
    point_values: true
    point_decimals: 2
    point_grid: [512, 512]

  mesh240:
    display: "Hail Swath 4-hr"
    mrms_dir: "MESH_Max_240min_00.50"
    data_png: { min: 0.0, max: 5.0 }
    units_in: "mm"
    units_out: "in"
    scale: 0.0393701
    clr: "mesh.clr"
    pal: ""
    group: "Hail"
    point_values: true
    point_decimals: 2
    point_grid: [512, 512]

  mesh1440:
    display: "Hail Swath 24-hr"
    mrms_dir: "MESH_Max_1440min_00.50"
    data_png: { min: 0.0, max: 5.0 }
    units_in: "mm"
    units_out: "in"
    scale: 0.0393701
    clr: "mesh.clr"
    pal: ""
    group: "Hail"
    point_values: true
    point_decimals: 2
    point_grid: [512, 512]

  posh:
    display: "Prob. of Severe Hail"
    mrms_dir: "POSH_00.50"
    data_png: { min: 0.0, max: 100.0 }
    units_in: "%"
    units_out: "%"
    clr: "posh.clr"
    pal: ""
    group: "Hail"
    point_values: true
    point_decimals: 0
    point_grid: [512, 512]

  # Echo Tops (MRMS stores km — scale to kft)
  et18:
    display: "Echo Top - 18 dBZ"
    mrms_dir: "EchoTop_18_00.50"
    data_png: { min: 0.0, max: 75.0 }
    units_in: "km"
    units_out: "kft"
    scale: 3.28084
    clr: "etops.clr"
    pal: ""
    group: "Echo Tops"
    point_values: true
    point_decimals: 0
    point_grid: [512, 512]

  et30:
    display: "Echo Top - 30 dBZ"
    mrms_dir: "EchoTop_30_00.50"
    data_png: { min: 0.0, max: 75.0 }
    units_in: "km"
    units_out: "kft"
    scale: 3.28084
    clr: "etops.clr"
    pal: ""
    group: "Echo Tops"
    point_values: true
    point_decimals: 0
    point_grid: [512, 512]

  et50:
    display: "Echo Top - 50 dBZ"
    mrms_dir: "EchoTop_50_00.50"
    data_png: { min: 0.0, max: 75.0 }
    units_in: "km"
    units_out: "kft"
    scale: 3.28084
    clr: "etops.clr"
    pal: ""
    group: "Echo Tops"
    point_values: true
    point_decimals: 0
    point_grid: [512, 512]

  et60:
    display: "Echo Top - 60 dBZ"
    mrms_dir: "EchoTop_60_00.50"
    data_png: { min: 0.0, max: 75.0 }
    units_in: "km"
    units_out: "kft"
    scale: 3.28084
    clr: "etops.clr"
    pal: ""
    group: "Echo Tops"
    point_values: true
    point_decimals: 0
    point_grid: [512, 512]

  # VIL family
  vil:
    display: "Vertically Integrated Liquid"
    mrms_dir: "VIL_00.50"
    data_png: { min: 0.0, max: 100.0 }
    units_in: "kg/m^2"
    units_out: "kg/m²"
    clr: "vil.clr"
    pal: ""
    group: "VIL"
    point_values: true
    point_decimals: 1
    point_grid: [512, 512]

  vild:
    display: "VIL Density"
    mrms_dir: "VIL_Density_00.50"
    data_png: { min: 0.0, max: 8.0 }
    units_in: "g/m^3"
    units_out: "g/m³"
    clr: "vild.clr"
    pal: ""
    group: "VIL"
    point_values: true
    point_decimals: 1
    point_grid: [512, 512]

  vii:
    display: "Vertically Integrated Ice"
    mrms_dir: "VII_00.50"
    data_png: { min: 0.0, max: 60.0 }
    units_in: "kg/m^2"
    units_out: "kg/m²"
    clr: "vii.clr"
    pal: ""
    group: "VIL"
    point_values: true
    point_decimals: 1
    point_grid: [512, 512]

  # Rotation tracks (low-level 0-2 km AGL azimuthal shear max)
  rot30:
    display: "Rotation Track 30-min"
    mrms_dir: "RotationTrack30min_00.50"
    data_png: { min: 0.0, max: 0.03 }
    units_in: "s^-1"
    units_out: "s⁻¹"
    clr: "rot.clr"
    pal: ""
    group: "Rotation"
    point_values: true
    point_decimals: 3
    point_grid: [512, 512]

  rot60:
    display: "Rotation Track 1-hr"
    mrms_dir: "RotationTrack60min_00.50"
    data_png: { min: 0.0, max: 0.03 }
    units_in: "s^-1"
    units_out: "s⁻¹"
    clr: "rot.clr"
    pal: ""
    group: "Rotation"
    point_values: true
    point_decimals: 3
    point_grid: [512, 512]

  rot240:
    display: "Rotation Track 4-hr"
    mrms_dir: "RotationTrack240min_00.50"
    data_png: { min: 0.0, max: 0.03 }
    units_in: "s^-1"
    units_out: "s⁻¹"
    clr: "rot.clr"
    pal: ""
    group: "Rotation"
    point_values: true
    point_decimals: 3
    point_grid: [512, 512]

  rot1440:
    display: "Rotation Track 24-hr"
    mrms_dir: "RotationTrack1440min_00.50"
    data_png: { min: 0.0, max: 0.03 }
    units_in: "s^-1"
    units_out: "s⁻¹"
    clr: "rot.clr"
    pal: ""
    group: "Rotation"
    point_values: true
    point_decimals: 3
    point_grid: [512, 512]

  # FLASH flood suite (10-min cadence, hydrologic model output)
  soilsat:
    display: "Soil Saturation (CREST)"
    mrms_dir: "FLASH_CREST_MAXSOILSAT_00.00"
    data_png: { min: 0.0, max: 100.0 }
    units_in: "%"
    units_out: "%"
    clr: "soilsat.clr"
    pal: ""
    group: "Flooding"
    point_values: true
    point_decimals: 0
    point_grid: [512, 512]

  unitq:
    display: "Unit Streamflow (CREST)"
    mrms_dir: "FLASH_CREST_MAXUNITSTREAMFLOW_00.00"
    data_png: { min: 0.0, max: 40.0 }
    units_in: "m^3/s/km^2"
    units_out: "m³/s/km²"
    clr: "unitq.clr"
    pal: ""
    group: "Flooding"
    point_values: true
    point_decimals: 2
    point_grid: [512, 512]

  ari01h:
    display: "Rainfall Recurrence 1-hr"
    mrms_dir: "FLASH_QPE_ARI01H_00.00"
    data_png: { min: 0.0, max: 500.0 }
    units_in: "years"
    units_out: "yr"
    clr: "ari.clr"
    pal: ""
    group: "Flooding"
    point_values: true
    point_decimals: 0
    point_grid: [512, 512]

  arimax:
    display: "Rainfall Recurrence Max"
    mrms_dir: "FLASH_QPE_ARIMAX_00.00"
    data_png: { min: 0.0, max: 500.0 }
    units_in: "years"
    units_out: "yr"
    clr: "ari.clr"
    pal: ""
    group: "Flooding"
    point_values: true
    point_decimals: 0
    point_grid: [512, 512]

  ffg01h:
    display: "QPE-to-FFG Ratio 1-hr"
    mrms_dir: "FLASH_QPE_FFG01H_00.00"
    data_png: { min: 0.0, max: 4.0 }
    units_in: "ratio"
    units_out: "x FFG"
    clr: "ffg.clr"
    pal: ""
    group: "Flooding"
    point_values: true
    point_decimals: 2
    point_grid: [512, 512]

  ffgmax:
    display: "QPE-to-FFG Ratio Max"
    mrms_dir: "FLASH_QPE_FFGMAX_00.00"
    data_png: { min: 0.0, max: 4.0 }
    units_in: "ratio"
    units_out: "x FFG"
    clr: "ffg.clr"
    pal: ""
    group: "Flooding"
    point_values: true
    point_decimals: 2
    point_grid: [512, 512]

  # ── MRMS catalog round 2: full QPE family + extended FLASH ──

  rate:
    display: "Precip Rate"
    mrms_dir: "PrecipRate_00.00"
    data_png: { min: 0.0, max: 6.0 }
    units_in: "mm/hr"
    units_out: "in/hr"
    scale: 0.0393701
    clr: "qpe.clr"
    pal: ""
    group: "Precipitation"
    point_values: true
    point_decimals: 2
    point_grid: [512, 512]

  rq15m:
    display: "QPE Radar-Only 15-min"
    mrms_dir: "RadarOnly_QPE_15M_00.00"
    data_png: { min: 0.0, max: 3.0 }
    units_in: "mm"
    units_out: "in"
    scale: 0.0393701
    clr: "qpe.clr"
    pal: ""
    group: "Precipitation"
    point_values: true
    point_decimals: 2
    point_grid: [512, 512]

  rq01h:
    display: "QPE Radar-Only 1-hour"
    mrms_dir: "RadarOnly_QPE_01H_00.00"
    data_png: { min: 0.0, max: 6.0 }
    units_in: "mm"
    units_out: "in"
    scale: 0.0393701
    clr: "qpe.clr"
    pal: ""
    group: "Precipitation"
    point_values: true
    point_decimals: 2
    point_grid: [512, 512]

  rq03h:
    display: "QPE Radar-Only 3-hour"
    mrms_dir: "RadarOnly_QPE_03H_00.00"
    data_png: { min: 0.0, max: 8.0 }
    units_in: "mm"
    units_out: "in"
    scale: 0.0393701
    clr: "qpe.clr"
    pal: ""
    group: "Precipitation"
    point_values: true
    point_decimals: 2
    point_grid: [512, 512]

  rq06h:
    display: "QPE Radar-Only 6-hour"
    mrms_dir: "RadarOnly_QPE_06H_00.00"
    data_png: { min: 0.0, max: 10.0 }
    units_in: "mm"
    units_out: "in"
    scale: 0.0393701
    clr: "qpe.clr"
    pal: ""
    group: "Precipitation"
    point_values: true
    point_decimals: 2
    point_grid: [512, 512]

  rq12h:
    display: "QPE Radar-Only 12-hour"
    mrms_dir: "RadarOnly_QPE_12H_00.00"
    data_png: { min: 0.0, max: 15.0 }
    units_in: "mm"
    units_out: "in"
    scale: 0.0393701
    clr: "qpe.clr"
    pal: ""
    group: "Precipitation"
    point_values: true
    point_decimals: 2
    point_grid: [512, 512]

  rq24h:
    display: "QPE Radar-Only 24-hour"
    mrms_dir: "RadarOnly_QPE_24H_00.00"
    data_png: { min: 0.0, max: 20.0 }
    units_in: "mm"
    units_out: "in"
    scale: 0.0393701
    clr: "qpe.clr"
    pal: ""
    group: "Precipitation"
    point_values: true
    point_decimals: 2
    point_grid: [512, 512]

  rq48h:
    display: "QPE Radar-Only 48-hour"
    mrms_dir: "RadarOnly_QPE_48H_00.00"
    data_png: { min: 0.0, max: 25.0 }
    units_in: "mm"
    units_out: "in"
    scale: 0.0393701
    clr: "qpe.clr"
    pal: ""
    group: "Precipitation"
    point_values: true
    point_decimals: 2
    point_grid: [512, 512]

  rq72h:
    display: "QPE Radar-Only 72-hour"
    mrms_dir: "RadarOnly_QPE_72H_00.00"
    data_png: { min: 0.0, max: 30.0 }
    units_in: "mm"
    units_out: "in"
    scale: 0.0393701
    clr: "qpe.clr"
    pal: ""
    group: "Precipitation"
    point_values: true
    point_decimals: 2
    point_grid: [512, 512]

  rq12z:
    display: "QPE Radar-Only Since 12Z"
    mrms_dir: "RadarOnly_QPE_Since12Z_00.00"
    data_png: { min: 0.0, max: 20.0 }
    units_in: "mm"
    units_out: "in"
    scale: 0.0393701
    clr: "qpe.clr"
    pal: ""
    group: "Precipitation"
    point_values: true
    point_decimals: 2
    point_grid: [512, 512]

  ms01h1:
    display: "QPE Multi-Sensor 1-hour Pass 1"
    mrms_dir: "MultiSensor_QPE_01H_Pass1_00.00"
    data_png: { min: 0.0, max: 6.0 }
    units_in: "mm"
    units_out: "in"
    scale: 0.0393701
    clr: "qpe.clr"
    pal: ""
    group: "Precip Multi-Sensor"
    point_values: true
    point_decimals: 2
    point_grid: [512, 512]

  ms01h2:
    display: "QPE Multi-Sensor 1-hour Pass 2"
    mrms_dir: "MultiSensor_QPE_01H_Pass2_00.00"
    data_png: { min: 0.0, max: 6.0 }
    units_in: "mm"
    units_out: "in"
    scale: 0.0393701
    clr: "qpe.clr"
    pal: ""
    group: "Precip Multi-Sensor"
    point_values: true
    point_decimals: 2
    point_grid: [512, 512]

  ms03h1:
    display: "QPE Multi-Sensor 3-hour Pass 1"
    mrms_dir: "MultiSensor_QPE_03H_Pass1_00.00"
    data_png: { min: 0.0, max: 8.0 }
    units_in: "mm"
    units_out: "in"
    scale: 0.0393701
    clr: "qpe.clr"
    pal: ""
    group: "Precip Multi-Sensor"
    point_values: true
    point_decimals: 2
    point_grid: [512, 512]

  ms03h2:
    display: "QPE Multi-Sensor 3-hour Pass 2"
    mrms_dir: "MultiSensor_QPE_03H_Pass2_00.00"
    data_png: { min: 0.0, max: 8.0 }
    units_in: "mm"
    units_out: "in"
    scale: 0.0393701
    clr: "qpe.clr"
    pal: ""
    group: "Precip Multi-Sensor"
    point_values: true
    point_decimals: 2
    point_grid: [512, 512]

  ms06h1:
    display: "QPE Multi-Sensor 6-hour Pass 1"
    mrms_dir: "MultiSensor_QPE_06H_Pass1_00.00"
    data_png: { min: 0.0, max: 10.0 }
    units_in: "mm"
    units_out: "in"
    scale: 0.0393701
    clr: "qpe.clr"
    pal: ""
    group: "Precip Multi-Sensor"
    point_values: true
    point_decimals: 2
    point_grid: [512, 512]

  ms06h2:
    display: "QPE Multi-Sensor 6-hour Pass 2"
    mrms_dir: "MultiSensor_QPE_06H_Pass2_00.00"
    data_png: { min: 0.0, max: 10.0 }
    units_in: "mm"
    units_out: "in"
    scale: 0.0393701
    clr: "qpe.clr"
    pal: ""
    group: "Precip Multi-Sensor"
    point_values: true
    point_decimals: 2
    point_grid: [512, 512]

  ms12h1:
    display: "QPE Multi-Sensor 12-hour Pass 1"
    mrms_dir: "MultiSensor_QPE_12H_Pass1_00.00"
    data_png: { min: 0.0, max: 15.0 }
    units_in: "mm"
    units_out: "in"
    scale: 0.0393701
    clr: "qpe.clr"
    pal: ""
    group: "Precip Multi-Sensor"
    point_values: true
    point_decimals: 2
    point_grid: [512, 512]

  ms12h2:
    display: "QPE Multi-Sensor 12-hour Pass 2"
    mrms_dir: "MultiSensor_QPE_12H_Pass2_00.00"
    data_png: { min: 0.0, max: 15.0 }
    units_in: "mm"
    units_out: "in"
    scale: 0.0393701
    clr: "qpe.clr"
    pal: ""
    group: "Precip Multi-Sensor"
    point_values: true
    point_decimals: 2
    point_grid: [512, 512]

  ms24h1:
    display: "QPE Multi-Sensor 24-hour Pass 1"
    mrms_dir: "MultiSensor_QPE_24H_Pass1_00.00"
    data_png: { min: 0.0, max: 20.0 }
    units_in: "mm"
    units_out: "in"
    scale: 0.0393701
    clr: "qpe.clr"
    pal: ""
    group: "Precip Multi-Sensor"
    point_values: true
    point_decimals: 2
    point_grid: [512, 512]

  ms24h2:
    display: "QPE Multi-Sensor 24-hour Pass 2"
    mrms_dir: "MultiSensor_QPE_24H_Pass2_00.00"
    data_png: { min: 0.0, max: 20.0 }
    units_in: "mm"
    units_out: "in"
    scale: 0.0393701
    clr: "qpe.clr"
    pal: ""
    group: "Precip Multi-Sensor"
    point_values: true
    point_decimals: 2
    point_grid: [512, 512]

  ms48h1:
    display: "QPE Multi-Sensor 48-hour Pass 1"
    mrms_dir: "MultiSensor_QPE_48H_Pass1_00.00"
    data_png: { min: 0.0, max: 25.0 }
    units_in: "mm"
    units_out: "in"
    scale: 0.0393701
    clr: "qpe.clr"
    pal: ""
    group: "Precip Multi-Sensor"
    point_values: true
    point_decimals: 2
    point_grid: [512, 512]

  ms48h2:
    display: "QPE Multi-Sensor 48-hour Pass 2"
    mrms_dir: "MultiSensor_QPE_48H_Pass2_00.00"
    data_png: { min: 0.0, max: 25.0 }
    units_in: "mm"
    units_out: "in"
    scale: 0.0393701
    clr: "qpe.clr"
    pal: ""
    group: "Precip Multi-Sensor"
    point_values: true
    point_decimals: 2
    point_grid: [512, 512]

  ms72h1:
    display: "QPE Multi-Sensor 72-hour Pass 1"
    mrms_dir: "MultiSensor_QPE_72H_Pass1_00.00"
    data_png: { min: 0.0, max: 30.0 }
    units_in: "mm"
    units_out: "in"
    scale: 0.0393701
    clr: "qpe.clr"
    pal: ""
    group: "Precip Multi-Sensor"
    point_values: true
    point_decimals: 2
    point_grid: [512, 512]

  ms72h2:
    display: "QPE Multi-Sensor 72-hour Pass 2"
    mrms_dir: "MultiSensor_QPE_72H_Pass2_00.00"
    data_png: { min: 0.0, max: 30.0 }
    units_in: "mm"
    units_out: "in"
    scale: 0.0393701
    clr: "qpe.clr"
    pal: ""
    group: "Precip Multi-Sensor"
    point_values: true
    point_decimals: 2
    point_grid: [512, 512]

  streamflow:
    display: "Streamflow (CREST)"
    mrms_dir: "FLASH_CREST_MAXSTREAMFLOW_00.00"
    data_png: { min: 0.0, max: 3000.0 }
    units_in: "m^3/s"
    units_out: "m³/s"
    clr: "qpe.clr"
    pal: ""
    group: "Flooding"
    point_values: true
    point_decimals: 0
    point_grid: [512, 512]

  sacsoil:
    display: "Soil Saturation (SAC)"
    mrms_dir: "FLASH_SAC_MAXSOILSAT_00.00"
    data_png: { min: 0.0, max: 100.0 }
    units_in: "%"
    units_out: "%"
    clr: "qpe.clr"
    pal: ""
    group: "Flooding"
    point_values: true
    point_decimals: 0
    point_grid: [512, 512]

  sacflow:
    display: "Streamflow (SAC)"
    mrms_dir: "FLASH_SAC_MAXSTREAMFLOW_00.00"
    data_png: { min: 0.0, max: 3000.0 }
    units_in: "m^3/s"
    units_out: "m³/s"
    clr: "qpe.clr"
    pal: ""
    group: "Flooding"
    point_values: true
    point_decimals: 0
    point_grid: [512, 512]

  sacunitq:
    display: "Unit Streamflow (SAC)"
    mrms_dir: "FLASH_SAC_MAXUNITSTREAMFLOW_00.00"
    data_png: { min: 0.0, max: 40.0 }
    units_in: "m^3/s/km^2"
    units_out: "m³/s/km²"
    clr: "qpe.clr"
    pal: ""
    group: "Flooding"
    point_values: true
    point_decimals: 2
    point_grid: [512, 512]

  hpflow:
    display: "Streamflow (Hydrophobic)"
    mrms_dir: "FLASH_HP_MAXSTREAMFLOW_00.00"
    data_png: { min: 0.0, max: 3000.0 }
    units_in: "m^3/s"
    units_out: "m³/s"
    clr: "qpe.clr"
    pal: ""
    group: "Flooding"
    point_values: true
    point_decimals: 0
    point_grid: [512, 512]

  hpunitq:
    display: "Unit Streamflow (Hydrophobic)"
    mrms_dir: "FLASH_HP_MAXUNITSTREAMFLOW_00.00"
    data_png: { min: 0.0, max: 40.0 }
    units_in: "m^3/s/km^2"
    units_out: "m³/s/km²"
    clr: "qpe.clr"
    pal: ""
    group: "Flooding"
    point_values: true
    point_decimals: 2
    point_grid: [512, 512]

  ari30m:
    display: "Rainfall Recurrence 30-min"
    mrms_dir: "FLASH_QPE_ARI30M_00.00"
    data_png: { min: 0.0, max: 500.0 }
    units_in: "years"
    units_out: "yr"
    clr: "qpe.clr"
    pal: ""
    group: "Flooding"
    point_values: true
    point_decimals: 0
    point_grid: [512, 512]

  ari03h:
    display: "Rainfall Recurrence 3-hr"
    mrms_dir: "FLASH_QPE_ARI03H_00.00"
    data_png: { min: 0.0, max: 500.0 }
    units_in: "years"
    units_out: "yr"
    clr: "qpe.clr"
    pal: ""
    group: "Flooding"
    point_values: true
    point_decimals: 0
    point_grid: [512, 512]

  ari06h:
    display: "Rainfall Recurrence 6-hr"
    mrms_dir: "FLASH_QPE_ARI06H_00.00"
    data_png: { min: 0.0, max: 500.0 }
    units_in: "years"
    units_out: "yr"
    clr: "qpe.clr"
    pal: ""
    group: "Flooding"
    point_values: true
    point_decimals: 0
    point_grid: [512, 512]

  ari12h:
    display: "Rainfall Recurrence 12-hr"
    mrms_dir: "FLASH_QPE_ARI12H_00.00"
    data_png: { min: 0.0, max: 500.0 }
    units_in: "years"
    units_out: "yr"
    clr: "qpe.clr"
    pal: ""
    group: "Flooding"
    point_values: true
    point_decimals: 0
    point_grid: [512, 512]

  ari24h:
    display: "Rainfall Recurrence 24-hr"
    mrms_dir: "FLASH_QPE_ARI24H_00.00"
    data_png: { min: 0.0, max: 500.0 }
    units_in: "years"
    units_out: "yr"
    clr: "qpe.clr"
    pal: ""
    group: "Flooding"
    point_values: true
    point_decimals: 0
    point_grid: [512, 512]

  ffg03h:
    display: "QPE-to-FFG Ratio 3-hr"
    mrms_dir: "FLASH_QPE_FFG03H_00.00"
    data_png: { min: 0.0, max: 4.0 }
    units_in: "ratio"
    units_out: "x FFG"
    clr: "qpe.clr"
    pal: ""
    group: "Flooding"
    point_values: true
    point_decimals: 2
    point_grid: [512, 512]

  ffg06h:
    display: "QPE-to-FFG Ratio 6-hr"
    mrms_dir: "FLASH_QPE_FFG06H_00.00"
    data_png: { min: 0.0, max: 4.0 }
    units_in: "ratio"
    units_out: "x FFG"
    clr: "qpe.clr"
    pal: ""
    group: "Flooding"
    point_values: true
    point_decimals: 2
    point_grid: [512, 512]

  # ── MRMS catalog round 3: reflectivity layers, hail/VIL swaths, rotation family ──

  rala:
    display: "Reflectivity At Lowest Altitude"
    mrms_dir: "MergedReflectivityAtLowestAltitude_00.50"
    data_png: { min: -30.0, max: 80.0 }
    units_in: "dBZ"
    units_out: "dBZ"
    sentinel_lt: -35.0
    clr: "qpe.clr"
    pal: ""
    group: "Reflectivity Layers"
    point_values: true
    point_decimals: 0
    point_grid: [512, 512]

  lcrlow:
    display: "Reflectivity Low (0-24 kft)"
    mrms_dir: "LayerCompositeReflectivity_Low_00.50"
    data_png: { min: -30.0, max: 80.0 }
    units_in: "dBZ"
    units_out: "dBZ"
    sentinel_lt: -35.0
    clr: "qpe.clr"
    pal: ""
    group: "Reflectivity Layers"
    point_values: true
    point_decimals: 0
    point_grid: [512, 512]

  lcrhigh:
    display: "Reflectivity High (24-60 kft)"
    mrms_dir: "LayerCompositeReflectivity_High_00.50"
    data_png: { min: -30.0, max: 80.0 }
    units_in: "dBZ"
    units_out: "dBZ"
    sentinel_lt: -35.0
    clr: "qpe.clr"
    pal: ""
    group: "Reflectivity Layers"
    point_values: true
    point_decimals: 0
    point_grid: [512, 512]

  lcrsuper:
    display: "Reflectivity Super (33-60 kft)"
    mrms_dir: "LayerCompositeReflectivity_Super_00.50"
    data_png: { min: -30.0, max: 80.0 }
    units_in: "dBZ"
    units_out: "dBZ"
    sentinel_lt: -35.0
    clr: "qpe.clr"
    pal: ""
    group: "Reflectivity Layers"
    point_values: true
    point_decimals: 0
    point_grid: [512, 512]

  refl0c:
    display: "Reflectivity (0C)"
    mrms_dir: "Reflectivity_0C_00.50"
    data_png: { min: -30.0, max: 80.0 }
    units_in: "dBZ"
    units_out: "dBZ"
    sentinel_lt: -35.0
    clr: "qpe.clr"
    pal: ""
    group: "Reflectivity Layers"
    point_values: true
    point_decimals: 0
    point_grid: [512, 512]

  reflm5c:
    display: "Reflectivity (-5C)"
    mrms_dir: "Reflectivity_-5C_00.50"
    data_png: { min: -30.0, max: 80.0 }
    units_in: "dBZ"
    units_out: "dBZ"
    sentinel_lt: -35.0
    clr: "qpe.clr"
    pal: ""
    group: "Reflectivity Layers"
    point_values: true
    point_decimals: 0
    point_grid: [512, 512]

  reflm10c:
    display: "Reflectivity (-10C)"
    mrms_dir: "Reflectivity_-10C_00.50"
    data_png: { min: -30.0, max: 80.0 }
    units_in: "dBZ"
    units_out: "dBZ"
    sentinel_lt: -35.0
    clr: "qpe.clr"
    pal: ""
    group: "Reflectivity Layers"
    point_values: true
    point_decimals: 0
    point_grid: [512, 512]

  reflm15c:
    display: "Reflectivity (-15C)"
    mrms_dir: "Reflectivity_-15C_00.50"
    data_png: { min: -30.0, max: 80.0 }
    units_in: "dBZ"
    units_out: "dBZ"
    sentinel_lt: -35.0
    clr: "qpe.clr"
    pal: ""
    group: "Reflectivity Layers"
    point_values: true
    point_decimals: 0
    point_grid: [512, 512]

  reflm20c:
    display: "Reflectivity (-20C)"
    mrms_dir: "Reflectivity_-20C_00.50"
    data_png: { min: -30.0, max: 80.0 }
    units_in: "dBZ"
    units_out: "dBZ"
    sentinel_lt: -35.0
    clr: "qpe.clr"
    pal: ""
    group: "Reflectivity Layers"
    point_values: true
    point_decimals: 0
    point_grid: [512, 512]

  crefmax1h:
    display: "Composite Refl Hourly Max"
    mrms_dir: "CREF_1HR_MAX_00.50"
    data_png: { min: -30.0, max: 80.0 }
    units_in: "dBZ"
    units_out: "dBZ"
    sentinel_lt: -35.0
    clr: "qpe.clr"
    pal: ""
    group: "Reflectivity Layers"
    point_values: true
    point_decimals: 0
    point_grid: [512, 512]

  brefmax1h:
    display: "Base Refl Hourly Max"
    mrms_dir: "BREF_1HR_MAX_00.50"
    data_png: { min: -30.0, max: 80.0 }
    units_in: "dBZ"
    units_out: "dBZ"
    sentinel_lt: -35.0
    clr: "qpe.clr"
    pal: ""
    group: "Reflectivity Layers"
    point_values: true
    point_decimals: 0
    point_grid: [512, 512]

  reflhgt:
    display: "Reflectivity Height"
    mrms_dir: "HeightCompositeReflectivity_00.50"
    data_png: { min: 0.0, max: 75.0 }
    units_in: "km"
    units_out: "kft"
    scale: 3.28084
    clr: "qpe.clr"
    pal: ""
    group: "Reflectivity Layers"
    point_values: true
    point_decimals: 0
    point_grid: [512, 512]

  shi:
    display: "Severe Hail Index"
    mrms_dir: "SHI_00.50"
    data_png: { min: 0.0, max: 300.0 }
    units_in: "index"
    units_out: ""
    clr: "qpe.clr"
    pal: ""
    group: "Hail"
    point_values: true
    point_decimals: 0
    point_grid: [512, 512]

  mesh30:
    display: "Hail Swath 30-min"
    mrms_dir: "MESH_Max_30min_00.50"
    data_png: { min: 0.0, max: 5.0 }
    units_in: "mm"
    units_out: "in"
    scale: 0.0393701
    clr: "qpe.clr"
    pal: ""
    group: "Hail"
    point_values: true
    point_decimals: 2
    point_grid: [512, 512]

  mesh120:
    display: "Hail Swath 2-hr"
    mrms_dir: "MESH_Max_120min_00.50"
    data_png: { min: 0.0, max: 5.0 }
    units_in: "mm"
    units_out: "in"
    scale: 0.0393701
    clr: "qpe.clr"
    pal: ""
    group: "Hail"
    point_values: true
    point_decimals: 2
    point_grid: [512, 512]

  mesh360:
    display: "Hail Swath 6-hr"
    mrms_dir: "MESH_Max_360min_00.50"
    data_png: { min: 0.0, max: 5.0 }
    units_in: "mm"
    units_out: "in"
    scale: 0.0393701
    clr: "qpe.clr"
    pal: ""
    group: "Hail"
    point_values: true
    point_decimals: 2
    point_grid: [512, 512]

  vilsw120:
    display: "VIL Swath 2-hr"
    mrms_dir: "VIL_Max_120min_00.50"
    data_png: { min: 0.0, max: 100.0 }
    units_in: "kg/m^2"
    units_out: "kg/m²"
    clr: "qpe.clr"
    pal: ""
    group: "VIL"
    point_values: true
    point_decimals: 1
    point_grid: [512, 512]

  vilsw1440:
    display: "VIL Swath 24-hr"
    mrms_dir: "VIL_Max_1440min_00.50"
    data_png: { min: 0.0, max: 100.0 }
    units_in: "kg/m^2"
    units_out: "kg/m²"
    clr: "qpe.clr"
    pal: ""
    group: "VIL"
    point_values: true
    point_decimals: 1
    point_grid: [512, 512]

  rotnow:
    display: "Rotation (0-2 km) Instant"
    mrms_dir: "MergedAzShear_0-2kmAGL_00.50"
    data_png: { min: 0.0, max: 0.03 }
    units_in: "s^-1"
    units_out: "s⁻¹"
    sentinel_lt: -1.0
    clr: "qpe.clr"
    pal: ""
    group: "Rotation"
    point_values: true
    point_decimals: 3
    point_grid: [512, 512]

  rot120:
    display: "Rotation Track 2-hr"
    mrms_dir: "RotationTrack120min_00.50"
    data_png: { min: 0.0, max: 0.03 }
    units_in: "s^-1"
    units_out: "s⁻¹"
    clr: "qpe.clr"
    pal: ""
    group: "Rotation"
    point_values: true
    point_decimals: 3
    point_grid: [512, 512]

  rot360:
    display: "Rotation Track 6-hr"
    mrms_dir: "RotationTrack360min_00.50"
    data_png: { min: 0.0, max: 0.03 }
    units_in: "s^-1"
    units_out: "s⁻¹"
    clr: "qpe.clr"
    pal: ""
    group: "Rotation"
    point_values: true
    point_decimals: 3
    point_grid: [512, 512]

  rotmlnow:
    display: "Rotation (3-6 km) Instant"
    mrms_dir: "MergedAzShear_3-6kmAGL_00.50"
    data_png: { min: 0.0, max: 0.03 }
    units_in: "s^-1"
    units_out: "s⁻¹"
    sentinel_lt: -1.0
    clr: "qpe.clr"
    pal: ""
    group: "Rotation"
    point_values: true
    point_decimals: 3
    point_grid: [512, 512]

  rotml30:
    display: "Rotation ML Track 30-min"
    mrms_dir: "RotationTrackML30min_00.50"
    data_png: { min: 0.0, max: 0.03 }
    units_in: "s^-1"
    units_out: "s⁻¹"
    clr: "qpe.clr"
    pal: ""
    group: "Rotation"
    point_values: true
    point_decimals: 3
    point_grid: [512, 512]

  rotml60:
    display: "Rotation ML Track 1-hr"
    mrms_dir: "RotationTrackML60min_00.50"
    data_png: { min: 0.0, max: 0.03 }
    units_in: "s^-1"
    units_out: "s⁻¹"
    clr: "qpe.clr"
    pal: ""
    group: "Rotation"
    point_values: true
    point_decimals: 3
    point_grid: [512, 512]

  rotml120:
    display: "Rotation ML Track 2-hr"
    mrms_dir: "RotationTrackML120min_00.50"
    data_png: { min: 0.0, max: 0.03 }
    units_in: "s^-1"
    units_out: "s⁻¹"
    clr: "qpe.clr"
    pal: ""
    group: "Rotation"
    point_values: true
    point_decimals: 3
    point_grid: [512, 512]

  rotml240:
    display: "Rotation ML Track 4-hr"
    mrms_dir: "RotationTrackML240min_00.50"
    data_png: { min: 0.0, max: 0.03 }
    units_in: "s^-1"
    units_out: "s⁻¹"
    clr: "qpe.clr"
    pal: ""
    group: "Rotation"
    point_values: true
    point_decimals: 3
    point_grid: [512, 512]

  rotml360:
    display: "Rotation ML Track 6-hr"
    mrms_dir: "RotationTrackML360min_00.50"
    data_png: { min: 0.0, max: 0.03 }
    units_in: "s^-1"
    units_out: "s⁻¹"
    clr: "qpe.clr"
    pal: ""
    group: "Rotation"
    point_values: true
    point_decimals: 3
    point_grid: [512, 512]

  rotml1440:
    display: "Rotation ML Track 24-hr"
    mrms_dir: "RotationTrackML1440min_00.50"
    data_png: { min: 0.0, max: 0.03 }
    units_in: "s^-1"
    units_out: "s⁻¹"
    clr: "qpe.clr"
    pal: ""
    group: "Rotation"
    point_values: true
    point_decimals: 3
    point_grid: [512, 512]

models:
  HRRR:
    products:
      - refc
      - refd1km
      - t2m
      - td2m
      - wind10m
      - gust10m
      - mslp
      - sfccape
      - sbcin
      - precip1h
      - precipTotal
      # Cloud cover family + simulated satellite (2026-08-05)
      - tcc
      - lcc
      - mcc
      - hcc
      - irsat
      # Fire weather / convective hazards (2026-08-05)
      - smokesfc
      - smokeint
      - ltng
      # Severe-parameter forecasts (full range, scrubbable in the models tab;
      # also_meso keeps them in the Mesoanalysis picker). Listed AFTER the
      # core surface products deliberately: render_hrrr.sh walks this list in
      # order, so a tick that runs long still lands reflectivity/temp/wind
      # first and only trails on the severe set.
      - srh1
      - srh3
      - ehi1
      - ehi3
      - uh25
      # Former analysis-only meso set — promoted to full-range dual-role
      # 2026-08-05 (see the Mesoanalysis section header note)
      - mlcape
      - mucape
      - mlcin
      - shear6
      - stp
      - scp
      # Meso catalog v2 (SPC-style groups) — promoted 2026-08-05 too
      - cape3km
      - liftedIdx
      - lapse75
      - lclHgt
      - shear1
      - stormMotion
      - pwat
      - echoTops
      - ptype
      - frzLvl
      - rh2m
      - vis
      # Winter (2026-08-05) — mostly-blank in August by nature; palettes
      # get their real visual check next cold season
      - snod
      - asnow
      - frzr
    # Parallel render groups: the workflow's plan job turns this map into a
    # job matrix, one render job per group (render_hrrr.sh PRODUCT_FILTER).
    # RULES:
    #   * Every product in `products` must appear in EXACTLY one group —
    #     the plan job diffs the union against the products list and fails
    #     the tick loudly on drift (a product missing from every group
    #     would otherwise silently never render).
    #   * `core` holds the headline products (reflectivity/temp/wind) in
    #     its own job so a long severe-param backfill can never starve
    #     them — the old single-job "list order = priority" trick, made
    #     structural.
    #   * Groups are balanced for FULL-RANGE load (each product ≈ same
    #     per-frame gdal cost; derived products just fetch a few more
    #     GRIB messages).
    render_groups:
      core:     [refc, t2m, td2m, wind10m, gust10m, mslp, sfccape, precip1h]
      sky:      [tcc, lcc, mcc, hcc, irsat, refd1km]
      hazards:  [smokesfc, smokeint, ltng, uh25, sbcin]
      winter:   [snod, asnow, frzr, precipTotal]
      severe-a: [srh1, srh3, ehi1, ehi3, shear1, shear6, stormMotion]
      severe-b: [mlcape, mucape, mlcin, cape3km, liftedIdx, lapse75, lclHgt]
      enviro:   [stp, scp, pwat, echoTops, ptype, frzLvl, rh2m, vis]
    # CONUS bbox (Lambert -> reprojected to EPSG:3857 by gdalwarp at render time)
    bbox_lonlat: [-134.0, 21.0, -60.0, 53.0]
    # Smoothness over sharpness (decided after testing). Animation scrub
    # cost is dominated by per-frame texture size: 2048^2 ≈ 16 MB RGBA,
    # 3072^2 ≈ 36 MB, 4096^2 ≈ 64 MB. 4096 (native ~3 km) and even 3072
    # made scrubbing stutter on phones, so we're back at 2048 — the
    # original, which scrubbed smoothly. Tradeoff: ~6 km/px (a little soft)
    # vs the model's native 3 km. Keep the cubic resampling below (it
    # sharpens edges slightly at zero playback cost — purely a render-time
    # kernel). If sharpness ever matters more than smooth animation, bump
    # this back up (4096 = native ceiling; mobile-GPU texture cap ~4096).
    image_size: [2048, 2048]
    # Point-value sample grid (sample_point_values.py). 128x128 over the
    # CONUS bbox ≈ one number every ~0.6° lon; evenly spaced on-screen in
    # mercator. The app decimates by zoom with power-of-2 strides, so keep
    # this a power of two.
    point_grid: [128, 128]
    # f00..f18 always; f00..f48 on synoptic runs.
    forecast_hours_default: { start: 0, end: 18, step: 1 }
    forecast_hours_synoptic: { start: 0, end: 48, step: 1, runs: [0, 6, 12, 18] }
    # AWS public bucket (no auth needed)
    s3_bucket: "noaa-hrrr-bdp-pds"
    s3_key_template: "hrrr.{date}/conus/hrrr.t{run:02d}z.wrfsfcf{fh:02d}.grib2"
    # Picker metadata -> v1/models.json (build_models_index.py). Only
    # models carrying picker_group appear in the app's model picker;
    # display strings override the app's built-in fallback names.
    display: "HRRR 3km"
    picker_group: "CONUS"
    picker_order: 10
    runs_per_day: 24
    retain_runs: 5

  RRFS:
    # NOAA's next-gen 3 km rapid-refresh model (operational Aug 31 2026;
    # pre-operational rrfs_a feed until then). The 2dfld CONUS file carries
    # every HRRR surface product PLUS a running-total APCP accumulation, so
    # RRFS publishes precipTotal too. wind500 lives in the separate
    # prslev.3km.na file (different key template) — not rendered in v1.
    products:
      - refc
      - refd1km
      - t2m
      - td2m
      - wind10m
      - gust10m
      - mslp
      - sfccape
      - sbcin
      - precip1h
      - precipTotal
      # 2026-08-05 expansion (Pivotal parity, mirrors HRRR where the 2dfld
      # file carries the field — verified in the 12z f009 idx). NOT here
      # because 2dfld genuinely differs: lapse75/wind500 (no pressure
      # levels), irsat (no SBT), smoke (RRFS MASSDEN/COLMD are split into
      # per-species aerosol messages — the HRRR match would grab organic
      # matter only; needs its own product definitions if ever wanted).
      # ⚠ TCDC appears as instantaneous AND hour-ave messages here — the
      # anchored prefix matches both and gdal_translate -b 1 takes the
      # FIRST in message order, which is the instantaneous one. LTNG is
      # an hourly max (fh_min 1 on the shared entry).
      - tcc
      - lcc
      - mcc
      - hcc
      - ltng
      - srh1
      - srh3
      - ehi1
      - ehi3
      - uh25
      - mlcape
      - mucape
      - mlcin
      - shear1
      - shear6
      - stp
      - scp
      - cape3km
      - liftedIdx
      - lclHgt
      - stormMotion
      - pwat
      - echoTops
      - ptype
      - frzLvl
      - rh2m
      - vis
      - snod
      - asnow
      - frzr
    # Parallel render groups — same contract as HRRR's (plan job enforces
    # exact coverage of the products list). RRFS synoptic runs reach f84,
    # the longest sweep of the three CONUS models — keep groups small.
    #
    # Rebalanced 2026-08-13: `core` carried 8 products and set the pace for
    # every tick (47 min measured on a full f84 backfill, so most 15-min
    # cron ticks were superseded in the concurrency queue before starting).
    # Split into core+surface and trimmed the two 7-product severe groups,
    # so the longest job is now 6 products (~35 min worst case). Group
    # NAMES are labels only — nothing outside this file reads them.
    render_groups:
      core:     [refc, t2m, td2m, wind10m]
      surface:  [gust10m, mslp, sfccape, precip1h]
      sky:      [tcc, lcc, mcc, hcc, refd1km]
      severe-a: [srh1, srh3, ehi1, ehi3, shear1, shear6]
      severe-b: [mlcape, mucape, mlcin, cape3km, sbcin, liftedIdx]
      hazards:  [stp, scp, ltng, uh25, ptype, stormMotion]
      enviro:   [pwat, echoTops, frzLvl, rh2m, vis, lclHgt]
      winter:   [snod, asnow, frzr, precipTotal]
    # Same CONUS render box + raster size as HRRR (both 3 km native; see the
    # HRRR image_size note for the 2048-vs-4096 scrub-smoothness tradeoff).
    # RRFS's CONUS subset grid doesn't cover this box edge-to-edge — the warp
    # leaves out-of-grid pixels transparent, which is fine.
    bbox_lonlat: [-134.0, 21.0, -60.0, 53.0]
    image_size: [2048, 2048]
    point_grid: [128, 128]
    # Hourly runs to f18; 00/06/12/18z extend to f84 (verified against the
    # rrfs_a bucket, July 2026).
    forecast_hours_default: { start: 0, end: 18, step: 1 }
    forecast_hours_synoptic: { start: 0, end: 84, step: 1, runs: [0, 6, 12, 18] }
    # ── NOMADS BRIDGE (2026-08-13 → cutover 2026-10-06) ────────────────
    # NOAA froze the rrfs_a/rrfs_public prototype feeds at 2026-08-12 11z
    # (SCN 26-48 pre-implementation parallel). The live source is NOMADS,
    # which serves no .idx — so mirror_rrfs.sh downloads each whole file,
    # extracts only our products' messages, and publishes slim grib+idx
    # pairs under v1/RRFS/_src/ on our own bucket. This template points
    # the (unchanged) byte-range pipeline at that mirror via the CDN.
    # Synoptic-only during the bridge: render_rrfs.sh skips other cycles
    # (NOMADS publishes them as sub-hourly files only — see mirror_rrfs.sh).
    base_url: "https://models.dgwaynes.com"
    s3_key_template: "v1/RRFS/_src/{date}{run:02d}/F{fh:03d}.grib2"
    display: "RRFS 3km"
    picker_group: "CONUS"
    picker_order: 12
    # ⚠ RESTORE AT CUTOVER (2026-10-06 12z): delete base_url + the mirror
    # job, re-enable hourly cycles in render_rrfs.sh, and swap back to the
    # operational bucket path (verify the prefix NCO actually ships):
    #   s3_bucket: "noaa-rrfs-pds"
    #   s3_key_template: "rrfs.{date}/{run:02d}/rrfs.t{run:02d}z.2dfld.3km.f{fh:03d}.conus.grib2"
    runs_per_day: 24
    retain_runs: 5

  OBS:
    # Observed MRMS QPE pseudo-model (see the qpe* product notes above).
    # "Runs" are hourly valid stamps (accumulation end hour), each with a
    # single F000 frame — forecast_hours 0-0 makes build_manifest.py's
    # expected set exactly [0].
    products:
      - qpe01h
      - qpe03h
      - qpe06h
      - qpe12h
      - qpe24h
      # MRMS observation catalog (render_mrms_obs.sh — mrms_dir products)
      - mesh
      - mesh60
      - mesh240
      - mesh1440
      - posh
      - et18
      - et30
      - et50
      - et60
      - vil
      - vild
      - vii
      - rot30
      - rot60
      - rot240
      - rot1440
      - soilsat
      - unitq
      - ari01h
      - arimax
      - ffg01h
      - ffgmax
      - rate
      - rq15m
      - rq01h
      - rq03h
      - rq06h
      - rq12h
      - rq24h
      - rq48h
      - rq72h
      - rq12z
      - ms01h1
      - ms01h2
      - ms03h1
      - ms03h2
      - ms06h1
      - ms06h2
      - ms12h1
      - ms12h2
      - ms24h1
      - ms24h2
      - ms48h1
      - ms48h2
      - ms72h1
      - ms72h2
      - streamflow
      - sacsoil
      - sacflow
      - sacunitq
      - hpflow
      - hpunitq
      - ari30m
      - ari03h
      - ari06h
      - ari12h
      - ari24h
      - ffg03h
      - ffg06h
      - rala
      - lcrlow
      - lcrhigh
      - lcrsuper
      - refl0c
      - reflm5c
      - reflm10c
      - reflm15c
      - reflm20c
      - crefmax1h
      - brefmax1h
      - reflhgt
      - shi
      - mesh30
      - mesh120
      - mesh360
      - vilsw120
      - vilsw1440
      - rotnow
      - rot120
      - rot360
      - rotmlnow
      - rotml30
      - rotml60
      - rotml120
      - rotml240
      - rotml360
      - rotml1440
    # MRMS CONUS grid coverage (lon 230.005-299.995E, lat 20.005-54.995N) —
    # narrower than the model CONUS box on the west edge; the app reads the
    # bbox from the manifest so overlay placement stays correct.
    bbox_lonlat: [-130.0, 20.0, -60.0, 55.0]
    # 4096 wide ≈ 1.9 km/px vs MRMS 1 km native — the crispness ceiling for
    # mobile GPU textures (max texture size ~4096). Unlike the models tab
    # there is NO animation scrubbing here (one static frame per range), so
    # the 2048 scrub-smoothness tradeoff doesn't apply. Height matches the
    # bbox's Web-Mercator aspect (70° lon / lat 20-55 → 0.6531).
    image_size: [4096, 2676]
    point_grid: [512, 512]
    forecast_hours_default: { start: 0, end: 0, step: 1 }
    s3_bucket: "noaa-mrms-pds"
    # Key template is informational — render_mrms_qpe.sh builds keys itself
    # (per-product prefix + Pass2/Pass1 fallback), it doesn't use decode_pipeline.sh.
    s3_key_template: "CONUS/{product}_Pass2_00.00/{date}/MRMS_{product}_Pass2_00.00_{date}-{run:02d}0000.grib2.gz"
    runs_per_day: 24
    # 26 hourly valid slots ≈ 24 h of history + slack — powers the app's
    # catalog playback loop (3/6/12/24-hr windows of hourly frames).
    retain_runs: 26

  RTMA:
    # RTMA surface-observation pseudo-model (render_rtma_obs.sh — NOT
    # decode_pipeline.sh). Each 2.5 km analysis publishes as its own
    # immutable run under a 12-digit YYYYMMDDHHMM stamp (Rapid-Update
    # cycles land every 15 min), because the app's frame disk-cache is
    # keyed by runStamp — see the script header for the full rationale.
    # forecast_hours 0-0 → expected set exactly [0], like OBS.
    products:
      - t2m
      - td2m
      - thetae
      - wind10m
      - gust10m
    # Same CONUS box as the forecast models / wind field so overlays line
    # up. 4096 wide ≈ 1.9 km/px vs RTMA 2.5 km native — single static
    # frame (no scrub), so the models-tab 2048 tradeoff doesn't apply;
    # height matches the bbox's Web-Mercator aspect (74° lon / lat 21-53).
    bbox_lonlat: [-134.0, 21.0, -60.0, 53.0]
    image_size: [4096, 2283]
    point_grid: [512, 512]
    forecast_hours_default: { start: 0, end: 0, step: 1 }
    s3_bucket: "noaa-rtma-pds"
    # Informational — render_rtma_obs.sh builds keys itself (RU-preferred
    # discovery with hourly fallback, mirroring wind_field.py).
    s3_key_template: "rtma2p5_ru.{date}/rtma2p5_ru.t{run:04d}z.2dvaranl_ndfd.grb2"
    # 96 RU analyses/day; retain 12 ≈ 3 h of history (the layer only ever
    # shows the newest — retention is just cold-start slack + debugging).
    runs_per_day: 96
    retain_runs: 12

  NAM:
    # NAM 3 km CONUS nest ("NAM NEST") — the chaser-standard day-2 CAM:
    # hourly output to f60 on all four cycles bridges HRRR's 18/48 h limit.
    # idx gotchas found on integration (2026-07-12):
    #   * U/V pairs are packed as SUBFIELD idx lines (624.1/624.2, one
    #     byte offset) — see compute_ranges in decode_pipeline.sh.
    #   * REFC uses the RRFS-style "(considered as a single layer)" text
    #     (prefix match already handles it).
    #   * APCP is 3-hour-reset buckets → precip3h with fh_step, and no
    #     running total → no precipTotal.
    # ⚠ NAM is slated for retirement after RRFS's operational cutover
    # (2026-08-31) — keep until NOAA actually stops publishing; symptom
    # will be runs silently going stale (idx HEAD 404s by design).
    products:
      - refc
      - refd1km
      - t2m
      - td2m
      - wind10m
      - gust10m
      - mslp
      - sfccape
      - sbcin
      - precip3h
      # 2026-08-05 expansion (Pivotal parity, mirrors HRRR where the
      # conusnest file carries the field — verified in the 12z f09 idx).
      # NOT here because the nest genuinely lacks them: cape3km, shear1
      # (no VUCSH 0-1000), asnow/frzr (no accum fields), smoke, SBT/irsat.
      # No precipTotal either (3-hour-reset buckets only — see precip3h).
      - tcc
      - lcc
      - mcc
      - hcc
      - ltng
      # Severe-parameter forecasts. The whole point of carrying these on NAM
      # is range: HRRR stops at f18 (f48 synoptic), NAM runs hourly to f60 on
      # every cycle, so this is the day-2 severe view. NAM has no `also_meso`
      # consequence worth worrying about — the app's Mesoanalysis layer reads
      # the HRRR manifest only.
      - srh1
      - srh3
      - ehi1
      - ehi3
      - uh25
      - mlcape
      - mucape
      - mlcin
      - shear6
      - stp
      - scp
      - liftedIdx
      - lapse75
      - lclHgt
      - stormMotion
      - pwat
      - echoTops
      - ptype
      - frzLvl
      - rh2m
      - vis
      - snod
    # Parallel render groups — same contract as HRRR's (plan job enforces
    # exact coverage of the products list).
    render_groups:
      core:     [refc, t2m, td2m, wind10m, gust10m, mslp, sfccape, precip3h]
      sky:      [tcc, lcc, mcc, hcc, refd1km]
      severe-a: [srh1, srh3, ehi1, ehi3, shear6, stormMotion, uh25]
      severe-b: [mlcape, mucape, mlcin, sbcin, liftedIdx, lapse75, lclHgt]
      hazards:  [stp, scp, ltng, ptype, frzLvl]
      enviro:   [pwat, echoTops, rh2m, vis, snod]
    # Same CONUS render box + raster size as HRRR/RRFS (3 km native; see
    # the HRRR image_size note for the 2048 scrub-smoothness tradeoff).
    bbox_lonlat: [-134.0, 21.0, -60.0, 53.0]
    image_size: [2048, 2048]
    point_grid: [128, 128]
    # All four cycles publish the full hourly f00-f60 range (verified
    # against the bucket 2026-07-12: f60 exists, f61 does not).
    forecast_hours_default: { start: 0, end: 60, step: 1 }
    s3_bucket: "noaa-nam-pds"
    s3_key_template: "nam.{date}/nam.t{run:02d}z.conusnest.hiresf{fh:02d}.tm00.grib2"
    display: "NAM 3km"
    picker_group: "CONUS"
    picker_order: 11
    runs_per_day: 4
    runs: [0, 6, 12, 18]
    retain_runs: 5

  ECMWF:
    # ECMWF IFS open data ("the Euro"), 0.25° global — data.ecmwf.int,
    # free under CC-BY-4.0 (attribution: "Contains modified ECMWF IFS
    # open data"). Not an AWS bucket: base_url + index_format switch the
    # fetch path in decode_pipeline.sh to the JSON .index files.
    # No simulated reflectivity in the open-data set — the catalog leads
    # with MUCAPE instead.
    # Publish latency is long (~7-9 h after init; 00z today was still 404
    # at 07Z) — the newest-cycle sweep just idx-404s until it lands.
    # Steps are 3-hourly to f144 then 6-hourly to f240 (f147 404, f150
    # 200 — verified 2026-08-06); segments carry the full 10-day range.
    products:
      - capeMU
      - t2m
      - td2m
      - wind10m
      - gust10m
      - mslp
      - precipTotalM
      # Round 3 (2026-08-06): everything else the open-data index carries
      # that maps onto existing product designs — tccF is the 0-1-fraction
      # cloud-cover twin (see its entry), pwat rides tcwv, vv700 rides
      # 'w'. Still absent for real reasons: ECMWF 'ptype' uses WMO code-
      # table categories (different encoding than our derived 1-4 flags),
      # 'vo' is RELATIVE vorticity (vort500 is absolute), derived
      # products (lapse75, ehi, shear) need per-input ecmwf matching the
      # pipeline doesn't have, and 'sd' snow depth is metres of water
      # equivalent needing a density conversion.
      - tccF
      - pwat
      - wind925
      - wind850
      - wind700
      - wind500
      - wind300
      - wind250
      - wind200
      - rh925
      - rh850
      - rh700
      - rh500
      - vv700
    # Global grid; render the same CONUS box as the other models for
    # app-side overlay parity (a GLOBAL entry can come later).
    bbox_lonlat: [-134.0, 21.0, -60.0, 53.0]
    # 0.25° ≈ 22 km native — 1024 px over 74° lon matches GFS.
    image_size: [1024, 1024]
    point_grid: [128, 128]
    forecast_hours_default: { start: 0, end: 144, step: 3 }
    forecast_hours_segments:
      - { start: 0, end: 144, step: 3 }
      - { start: 150, end: 240, step: 6 }
    # oper stream = 00z and 12z only (06/18z live in the separate scda
    # stream with a 90 h range — not worth a second key template in v1).
    base_url: "https://data.ecmwf.int/forecasts"
    index_format: "ecmwf"
    s3_key_template: "{date}/{run:02d}z/ifs/0p25/oper/{date}{run:02d}0000-{fh}h-oper-fc.grib2"
    display: "ECMWF 25km"
    picker_group: "Global"
    picker_order: 22
    runs_per_day: 2
    runs: [0, 12]
    retain_runs: 4

  AIFS:
    # ECMWF's AIFS (AI Forecast System), single deterministic — the "AI
    # Euro". Same open-data endpoint + licence as ECMWF (CC-BY-4.0,
    # "Contains modified ECMWF AIFS open data"); all four cycles,
    # 6-hourly steps to f360 = 15 days (verified 2026-08-06: f360 200,
    # f366 404). Param set is the AI-model subset: NO gusts, CAPE,
    # simulated reflectivity, or pl relative humidity (only specific
    # humidity q); clouds are 0-1 fractions -> the *F twin entries.
    products:
      - t2m
      - td2m
      - wind10m
      - mslp
      - precipTotalM
      - tccF
      - lccF
      - mccF
      - hccF
      - wind925
      - wind850
      - wind700
      - wind500
      - wind300
      - wind250
      - wind200
      - vv700
    render_groups:
      core:   [t2m, td2m, wind10m, mslp, precipTotalM]
      clouds: [tccF, lccF, mccF, hccF]
      upper:  [wind925, wind850, wind700, wind500, wind300, wind250, wind200, vv700]
    bbox_lonlat: [-134.0, 21.0, -60.0, 53.0]
    image_size: [1024, 1024]
    point_grid: [128, 128]
    forecast_hours_default: { start: 0, end: 360, step: 6 }
    base_url: "https://data.ecmwf.int/forecasts"
    index_format: "ecmwf"
    s3_key_template: "{date}/{run:02d}z/aifs-single/0p25/oper/{date}{run:02d}0000-{fh}h-oper-fc.grib2"
    display: "AIFS 25km"
    picker_group: "Global"
    picker_order: 23
    runs_per_day: 4
    runs: [0, 6, 12, 18]
    retain_runs: 4

  GEFS:
    # GEFS ensemble MEAN (the geavg member), 0.5° pgrb2a subset, 6-hourly
    # to f384 — the 16-day trend view. Subset limits, verified against
    # the 00z f024 idx 2026-08-06: no DPT/GUST/VIS/REFC/ABSV/HGT-0C;
    # CAPE/CIN only as 180-0 mb ML (own entries); VVEL only at 850 mb;
    # TCDC is a 6-h AVERAGE with no f00 message (one red slot at f00 —
    # cosmetic); APCP is clean 6-h buckets (precip6h), no running total.
    products:
      - t2m
      - rh2m
      - wind10m
      - mslp
      - precip6h
      - pwat
      - tcc
      - snod
      - capeML180
      - cinML180
      - wind925
      - wind850
      - wind700
      - wind500
      - wind300
      - wind250
      - wind200
      - rh925
      - rh850
      - rh700
      - rh500
      - vv850
    render_groups:
      core:       [t2m, rh2m, wind10m, mslp, precip6h]
      enviro:     [pwat, tcc, snod, capeML180, cinML180]
      upper-wind: [wind925, wind850, wind700, wind500, wind300, wind250, wind200]
      upper-dyn:  [rh925, rh850, rh700, rh500, vv850]
    bbox_lonlat: [-134.0, 21.0, -60.0, 53.0]
    image_size: [1024, 1024]
    point_grid: [128, 128]
    forecast_hours_default: { start: 0, end: 384, step: 6 }
    s3_bucket: "noaa-gefs-pds"
    s3_key_template: "gefs.{date}/{run:02d}/atmos/pgrb2ap5/geavg.t{run:02d}z.pgrb2a.0p50.f{fh:03d}"
    display: "GEFS Mean 50km"
    picker_group: "Ensembles"
    picker_order: 30
    runs_per_day: 4
    runs: [0, 6, 12, 18]
    retain_runs: 4

  ICOND2:
    # DWD ICON-D2 (2.2 km Central-Europe CAM) — the om-reader pilot model,
    # sourced from the Open-Meteo AWS Open Data bucket's data_spatial
    # layout (s3://openmeteo, us-west-2, public, free egress) instead of a
    # GRIB feed: one .om file per valid time carries all ~93 variables.
    # Licence CC-BY-4.0 ("Weather data by Open-Meteo.com"); the underlying
    # DWD open data is CC-BY-4.0 as well. See scripts/om_extract.py.
    # 8 cycles/day (every 3 h), hourly f00-f48; first files land ~46 min
    # after init, full run complete ~init+80 min (measured 2026-08-13).
    # No simulated reflectivity — Open-Meteo doesn't ingest DWD's DBZ
    # fields, so the catalog leads with CAPE/updraft/LPI instead.
    # Values arrive unit-normalized (degC, m/s, hPa, mm) so most products
    # reuse the GRIB-path convert expressions; om_convert overrides the
    # exceptions (mslp arrives in hPa, cinML needs the sign flip).
    products:
      - t2m
      - td2m
      - rh2m
      - wind10m
      - gust10m
      - mslp
      - vis
      - precip1h
      - tcc
      - lcc
      - mcc
      - hcc
      - capeML
      - cinML
      - lpi
      - updraft
      - wind850
      - wind700
      - wind500
      - wind300
      - wind250
      - wind200
      - rh850
      - rh700
      - rh500
      - snod
      - frzLvl
    source_type: "openmeteo_spatial"
    om_model_path: "dwd_icon_d2"
    om_cycle_hours: 3
    display: "ICON-D2 2km"
    picker_group: "Europe"
    picker_order: 50
    # Grid-point-center extent from the bucket's meta.json crs_wkt BBOX
    # (43.18..58.08N, -3.94..20.34E, 1215x746 @ 0.02 deg regular WGS84).
    bbox_lonlat: [-3.94, 43.18, 20.34, 58.08]
    # Web-Mercator aspect of that bbox is ~1.02:1 (y span 0.4143 rad vs
    # x span 0.4238), so a near-square output; 1536 wide is a modest
    # upsample of the 1215-col native grid.
    image_size: [1536, 1502]
    point_grid: [128, 128]
    forecast_hours_default: { start: 0, end: 48, step: 1 }
    retain_runs: 5

  # ── Batch 2 om models (2026-08-14): regular-lat-lon global grids from
  # the Open-Meteo bucket, rendered to the standard CONUS bbox like GFS.
  # Per-model variable sets verified against latest.json the same day —
  # each products list contains ONLY fields that exist in that model's
  # spatial files (om_extract exit-3 skips guard against drift anyway).
  # NBM / HRRR-15min / HRDPS / RDPS are NOT here: their grids are
  # Lambert/rotated and need om_extract projection support first.

  AIGFS:
    # NOAA GraphCast-GFS ("AI GFS"), 0.25° global, 4 cycles/day.
    # 6-hourly f006-f384 — the model emits NO f000 step. Thin surface
    # set (no RH/gust/CAPE/vis) but a full pressure ladder.
    products:
      - t2m
      - mslp
      - wind10m
      - precip6h
      - tcc
      - lcc
      - mcc
      - hcc
      - wind850
      - wind700
      - wind500
      - wind300
      - wind250
      - wind200
      - rh850
      - rh700
      - rh500
    source_type: "openmeteo_spatial"
    om_model_path: "ncep_aigfs025"
    om_cycle_hours: 6
    display: "AI GFS 25km"
    picker_group: "Global"
    picker_order: 24
    bbox_lonlat: [-134.0, 21.0, -60.0, 53.0]
    image_size: [1024, 1024]
    point_grid: [128, 128]
    forecast_hours_default: { start: 6, end: 384, step: 6 }
    retain_runs: 4

  GFS013:
    # GFS at 0.13° ("GFS 13km") — the hourly GFS: f000-f120 hourly then
    # 3-hourly to f384. Surface-only variable set (no pressure levels,
    # no MSLP — surface pressure only in the source, so mslp is absent).
    # precip1h on the 3-hourly tail is a 3-h bucket (precip since the
    # previous frame) — same semantics the scrub bar already shows for
    # mixed-cadence models.
    products:
      - t2m
      - td2m
      - rh2m
      - wind10m
      - precip1h
      - tcc
      - lcc
      - mcc
      - hcc
      - snod
    source_type: "openmeteo_spatial"
    om_model_path: "ncep_gfs013"
    om_cycle_hours: 6
    display: "GFS 13km"
    picker_group: "Global"
    picker_order: 21
    bbox_lonlat: [-134.0, 21.0, -60.0, 53.0]
    image_size: [1536, 1536]
    point_grid: [128, 128]
    forecast_hours_segments:
      - { start: 0, end: 120, step: 1 }
      - { start: 123, end: 384, step: 3 }
    retain_runs: 4

  UKMO:
    # UK Met Office global deterministic, 10 km, 4 cycles/day, hourly
    # f000-f054 then 3-hourly to f060. Richest surface set of the batch
    # (CAPE/CIN/gust/vis) but 10m wind is a SPEED scalar (wind10mS) and
    # pressure-level wind is speed-only too (skipped for now).
    products:
      - t2m
      - td2m
      - rh2m
      - wind10mS
      - gust10m
      - mslp
      - vis
      - precip1h
      - tcc
      - lcc
      - mcc
      - hcc
      - sfccape
      - sbcin
      - rh850
      - rh700
      - rh500
    source_type: "openmeteo_spatial"
    om_model_path: "ukmo_global_deterministic_10km"
    om_cycle_hours: 6
    display: "UKMO 10km"
    picker_group: "Global"
    picker_order: 25
    bbox_lonlat: [-134.0, 21.0, -60.0, 53.0]
    image_size: [1024, 1024]
    point_grid: [128, 128]
    forecast_hours_segments:
      - { start: 0, end: 54, step: 1 }
      - { start: 57, end: 60, step: 3 }
    retain_runs: 4

  GDPS:
    # Environment Canada GEM global (GDPS), 15 km, 2 cycles/day (00/12z),
    # hourly f000-f084 then 3-hourly to f240. Total cloud only; 10m wind
    # is a speed scalar (wind10mS).
    products:
      - t2m
      - td2m
      - rh2m
      - wind10mS
      - gust10m
      - mslp
      - precip1h
      - tcc
      - snod
    source_type: "openmeteo_spatial"
    om_model_path: "cmc_gem_gdps_15km"
    om_cycle_hours: 12
    display: "GDPS 15km"
    picker_group: "Global"
    picker_order: 26
    bbox_lonlat: [-134.0, 21.0, -60.0, 53.0]
    image_size: [1024, 1024]
    point_grid: [128, 128]
    forecast_hours_segments:
      - { start: 0, end: 84, step: 1 }
      - { start: 87, end: 240, step: 3 }
    retain_runs: 4

  HGEFS:
    # NCEP Hurricane GEFS ensemble MEAN, 0.25°, 4 cycles/day, 6-hourly
    # f006-f240 (no f000, like AIGFS). The tropical-season trend view.
    products:
      - t2m
      - mslp
      - wind10m
      - precip6h
      - tcc
      - lcc
      - mcc
      - hcc
      - wind850
      - wind700
      - wind500
      - wind300
      - wind250
      - wind200
      - rh850
      - rh700
      - rh500
    source_type: "openmeteo_spatial"
    om_model_path: "ncep_hgefs025_ensemble_mean"
    om_cycle_hours: 6
    display: "HGEFS Mean 25km"
    picker_group: "Tropical"
    picker_order: 40
    bbox_lonlat: [-134.0, 21.0, -60.0, 53.0]
    image_size: [1024, 1024]
    point_grid: [128, 128]
    forecast_hours_default: { start: 6, end: 240, step: 6 }
    retain_runs: 4

  # ── Batch 3 om models (2026-08-14): Europe fill-out + Asia + Marine.
  # All regular-lat-lon grids verified against latest.json crs_wkt; per-
  # model bboxes are the meta.json grid-point extents (regional models)
  # or the standard CONUS box (globals + waves). Uncertain publish
  # cadences use the FINER plausible om_cycle_hours + a wider
  # om_cycles_back — a nonexistent intermediate cycle just gate-skips.

  ICON:
    # DWD ICON global, ingested at 0.25°. Hourly f00-f78, 3-hourly to
    # f180. Has CAPE (mixed-layer parcel per DWD) but NO CIN in the
    # spatial set, and no visibility.
    products:
      - t2m
      - td2m
      - rh2m
      - wind10m
      - gust10m
      - mslp
      - precip1h
      - tcc
      - lcc
      - mcc
      - hcc
      - capeML
      - snod
      - frzLvl
      - wind850
      - wind700
      - wind500
      - wind300
      - wind250
      - wind200
      - rh850
      - rh700
      - rh500
    source_type: "openmeteo_spatial"
    om_model_path: "dwd_icon"
    om_cycle_hours: 6
    display: "ICON 25km"
    picker_group: "Global"
    picker_order: 27
    bbox_lonlat: [-134.0, 21.0, -60.0, 53.0]
    image_size: [1024, 1024]
    point_grid: [128, 128]
    forecast_hours_segments:
      - { start: 0, end: 78, step: 1 }
      - { start: 81, end: 180, step: 3 }
    retain_runs: 4

  ICONEU:
    # DWD ICON-EU nest (~7 km). Hourly f00-f78, 3-hourly to f120.
    # Fullest Europe surface set of the batch (vis + CIN included).
    # DWD runs intermediate 3-hourly cycles with shorter range — the
    # finer cycle assumption + gates handles whichever exist.
    products:
      - t2m
      - td2m
      - rh2m
      - wind10m
      - gust10m
      - mslp
      - vis
      - precip1h
      - tcc
      - lcc
      - mcc
      - hcc
      - capeML
      - cinML
      - snod
      - frzLvl
      - wind850
      - wind700
      - wind500
      - wind300
      - wind250
      - wind200
      - rh850
      - rh700
      - rh500
    source_type: "openmeteo_spatial"
    om_model_path: "dwd_icon_eu"
    om_cycle_hours: 3
    om_cycles_back: 3
    display: "ICON-EU 7km"
    picker_group: "Europe"
    picker_order: 51
    bbox_lonlat: [-23.5, 29.5, 62.5, 70.5]
    image_size: [1536, 1250]
    point_grid: [128, 128]
    forecast_hours_segments:
      - { start: 0, end: 78, step: 1 }
      - { start: 81, end: 120, step: 3 }
    retain_runs: 4

  AROMEFR:
    # Météo-France AROME 0.025° (~2.5 km), hourly f00-f51. Runs land
    # 5x/day on an irregular schedule (00/03/06/12/18-ish) — the 3-h
    # cycle assumption + gates renders whichever cycles exist.
    products:
      - t2m
      - td2m
      - rh2m
      - wind10m
      - gust10m
      - mslp
      - precip1h
      - tcc
      - lcc
      - mcc
      - hcc
      - sfccape
      - wind850
      - wind700
      - wind500
      - wind300
      - wind250
      - wind200
      - rh850
      - rh700
      - rh500
    source_type: "openmeteo_spatial"
    om_model_path: "meteofrance_arome_france0025"
    om_cycle_hours: 3
    om_cycles_back: 3
    display: "AROME 2.5km"
    picker_group: "Europe"
    picker_order: 52
    bbox_lonlat: [-12.0, 37.5, 16.0, 55.4]
    image_size: [1280, 1200]
    point_grid: [128, 128]
    forecast_hours_default: { start: 0, end: 51, step: 1 }
    retain_runs: 4

  ARPEGE:
    # Météo-France ARPEGE global 0.25°. Hourly f00-f48, 3-hourly to f102.
    products:
      - t2m
      - td2m
      - rh2m
      - wind10m
      - gust10m
      - mslp
      - precip1h
      - tcc
      - lcc
      - mcc
      - hcc
      - sfccape
      - wind850
      - wind700
      - wind500
      - wind300
      - wind250
      - wind200
      - rh850
      - rh700
      - rh500
    source_type: "openmeteo_spatial"
    om_model_path: "meteofrance_arpege_world025"
    om_cycle_hours: 6
    display: "ARPEGE 25km"
    picker_group: "Global"
    picker_order: 28
    bbox_lonlat: [-134.0, 21.0, -60.0, 53.0]
    image_size: [1024, 1024]
    point_grid: [128, 128]
    forecast_hours_segments:
      - { start: 0, end: 48, step: 1 }
      - { start: 51, end: 102, step: 3 }
    retain_runs: 4

  ARPEGEEU:
    # ARPEGE Europe 0.1° — thin variable set but HOURLY all the way to
    # f102, the longest hourly scrub in the Europe section.
    products:
      - t2m
      - td2m
      - rh2m
      - wind10m
      - gust10m
      - mslp
      - precip1h
      - tcc
      - lcc
      - mcc
      - hcc
    source_type: "openmeteo_spatial"
    om_model_path: "meteofrance_arpege_europe"
    om_cycle_hours: 6
    display: "ARPEGE-EU 10km"
    picker_group: "Europe"
    picker_order: 53
    bbox_lonlat: [-32.0, 20.0, 42.0, 72.0]
    image_size: [1280, 1472]
    point_grid: [128, 128]
    forecast_hours_default: { start: 0, end: 102, step: 1 }
    retain_runs: 4

  ICON2I:
    # ItaliaMeteo/ARPAE ICON-2I (2.2 km Italy), hourly f00-f72, 2
    # cycles/day (00/12z). Speed-only 10m wind; carries LPI + ML
    # CAPE/CIN like its ICON-D2 sibling.
    products:
      - t2m
      - td2m
      - rh2m
      - wind10mS
      - gust10m
      - mslp
      - precip1h
      - tcc
      - lcc
      - mcc
      - hcc
      - capeML
      - cinML
      - lpi
      - snod
      - frzLvl
      - rh850
      - rh700
      - rh500
    source_type: "openmeteo_spatial"
    om_model_path: "italia_meteo_arpae_icon_2i"
    om_cycle_hours: 12
    display: "ICON-2I 2km"
    picker_group: "Europe"
    picker_order: 54
    bbox_lonlat: [3.0, 33.7, 22.0, 48.9]
    image_size: [1024, 1104]
    point_grid: [128, 128]
    forecast_hours_default: { start: 0, end: 72, step: 1 }
    retain_runs: 3

  AROMEAT:
    # GeoSphere Austria AROME (~2.5 km Alpine domain), hourly f00-f60.
    products:
      - t2m
      - td2m
      - rh2m
      - wind10mS
      - gust10m
      - mslp
      - precip1h
      - tcc
      - lcc
      - mcc
      - hcc
      - sfccape
      - sbcin
    source_type: "openmeteo_spatial"
    om_model_path: "geosphere_arome_austria"
    om_cycle_hours: 3
    om_cycles_back: 3
    display: "AROME-AT 2.5km"
    picker_group: "Europe"
    picker_order: 55
    bbox_lonlat: [5.498, 42.981, 22.102, 51.819]
    image_size: [1024, 810]
    point_grid: [128, 128]
    forecast_hours_default: { start: 0, end: 60, step: 1 }
    retain_runs: 4

  JMAMSM:
    # JMA MSM (5 km Japan), hourly f00-f39, 3-hourly cycles with LONG
    # ingest latency (~5-8 h behind was observed) — gates + a wide
    # window absorb it. Pairs with the Himawari W-Pacific satellite
    # region for the typhoon audience. No gust/CAPE in the set.
    products:
      - t2m
      - td2m
      - rh2m
      - wind10m
      - mslp
      - precip1h
      - tcc
      - lcc
      - mcc
      - hcc
    source_type: "openmeteo_spatial"
    om_model_path: "jma_msm"
    om_cycle_hours: 3
    om_cycles_back: 4
    display: "JMA MSM 5km"
    picker_group: "Asia"
    picker_order: 60
    bbox_lonlat: [120.0, 22.4, 150.0, 47.6]
    image_size: [1024, 1068]
    point_grid: [128, 128]
    forecast_hours_default: { start: 0, end: 39, step: 1 }
    retain_runs: 4

  JMAGSM:
    # JMA GSM global 0.5°, 6-hourly f00-f132, rendered over the West
    # Pacific typhoon basin (not CONUS) — it lives in the Asia section
    # next to MSM and the Himawari imagery.
    products:
      - t2m
      - td2m
      - rh2m
      - wind10m
      - mslp
      - precip6h
      - tcc
      - lcc
      - mcc
      - hcc
      - wind850
      - wind700
      - wind500
      - wind300
      - wind250
      - wind200
      - rh850
      - rh700
      - rh500
    source_type: "openmeteo_spatial"
    om_model_path: "jma_gsm"
    om_cycle_hours: 6
    om_cycles_back: 3
    display: "JMA GSM 50km"
    picker_group: "Asia"
    picker_order: 61
    bbox_lonlat: [100.0, 5.0, 170.0, 50.0]
    image_size: [1280, 968]
    point_grid: [128, 128]
    forecast_hours_default: { start: 0, end: 132, step: 6 }
    retain_runs: 4

  CMA:
    # CMA GRAPES/CMA-GFS global 0.125°, 3-hourly f00-f120. Surprisingly
    # rich surface set (vis, CAPE+CIN, snow depth, full ladders).
    products:
      - t2m
      - td2m
      - rh2m
      - wind10m
      - gust10m
      - mslp
      - vis
      - precip3h
      - tcc
      - lcc
      - mcc
      - hcc
      - sfccape
      - sbcin
      - snod
      - wind850
      - wind700
      - wind500
      - wind300
      - wind250
      - wind200
      - rh850
      - rh700
      - rh500
    source_type: "openmeteo_spatial"
    om_model_path: "cma_grapes_global"
    om_cycle_hours: 6
    display: "CMA 13km"
    picker_group: "Global"
    picker_order: 29
    bbox_lonlat: [-134.0, 21.0, -60.0, 53.0]
    image_size: [1024, 1024]
    point_grid: [128, 128]
    forecast_hours_default: { start: 0, end: 120, step: 3 }
    retain_runs: 4

  GFSWAVE:
    # NOAA GFS-Wave 0.25°, HOURLY f00-f120 then 3-hourly to f384,
    # rendered over the CONUS box (both coasts + Gulf + Great Lakes
    # fringe). Land is nodata → transparent, which reads correctly.
    products:
      - waveHgt
      - swellHgt
      - wavePeriod
    source_type: "openmeteo_spatial"
    om_model_path: "ncep_gfswave025"
    om_cycle_hours: 6
    display: "GFS Wave 25km"
    picker_group: "Marine"
    picker_order: 70
    bbox_lonlat: [-134.0, 21.0, -60.0, 53.0]
    image_size: [1024, 1024]
    point_grid: [128, 128]
    forecast_hours_segments:
      - { start: 0, end: 120, step: 1 }
      - { start: 123, end: 384, step: 3 }
    retain_runs: 4

  ECMWFWAM:
    # ECMWF WAM 0.25°, 3-hourly f00-f144. No swell split in the open
    # spatial set (height/period/direction only).
    products:
      - waveHgt
      - wavePeriod
    source_type: "openmeteo_spatial"
    om_model_path: "ecmwf_wam025"
    om_cycle_hours: 6
    display: "ECMWF Wave 25km"
    picker_group: "Marine"
    picker_order: 71
    bbox_lonlat: [-134.0, 21.0, -60.0, 53.0]
    image_size: [1024, 1024]
    point_grid: [128, 128]
    forecast_hours_default: { start: 0, end: 144, step: 3 }
    retain_runs: 4

  GWAM:
    # DWD GWAM global wave model, 3-hourly f00-f174, 2 cycles/day.
    products:
      - waveHgt
      - swellHgt
      - wavePeriod
    source_type: "openmeteo_spatial"
    om_model_path: "dwd_gwam"
    om_cycle_hours: 12
    display: "GWAM 25km"
    picker_group: "Marine"
    picker_order: 72
    bbox_lonlat: [-134.0, 21.0, -60.0, 53.0]
    image_size: [1024, 1024]
    point_grid: [128, 128]
    forecast_hours_default: { start: 0, end: 174, step: 3 }
    retain_runs: 3

  GFS:
    # 2026-08-06 expansion (Pivotal parity) — verified against the 12z
    # f000/f009/f012 pgrb2.0p25 idx. GFS genuinely lacks: HLCY 1000-0
    # (no srh1/ehi1), MXUPHL (no uh25), VUCSH (no shear1/shear6/scp),
    # the LCL-height message (no lclHgt/stp), LTNG, smoke, SBT, cape3km,
    # ASNOW/FRZR (snow depth only). TCDC/LCDC etc. publish instantaneous
    # + period-ave pairs — the instantaneous message sorts first, so the
    # anchored prefix + `-b 1` picks it, same as RRFS.
    products:
      - refc
      - refd1km
      - t2m
      - td2m
      - wind10m
      - gust10m
      - mslp
      - sfccape
      - sbcin
      - precip6h
      - precipTotal
      - tcc
      - lcc
      - mcc
      - hcc
      - ptype
      - srh3
      - ehi3
      - mlcape
      - mucape
      - mlcin
      - liftedIdx
      - lapse75
      - stormMotion
      - pwat
      - frzLvl
      - rh2m
      - vis
      - snod
      # Upper-air suite (2026-08-06 round 2): full wind ladder, RH,
      # dynamics, and the derived sfc-500mb bulk shear.
      - shearSfc500
      - wind925
      - wind850
      - wind700
      - wind500
      - wind300
      - wind250
      - wind200
      - rh925
      - rh850
      - rh700
      - rh500
      - vort500
      - vv700
    # Parallel render groups — same contract as HRRR's (plan job enforces
    # exact coverage). 1024² global-grid frames are ~4x cheaper than the
    # 3 km CONUS models', so groups can run a little wider.
    render_groups:
      core:       [refc, t2m, td2m, wind10m, gust10m, mslp, sfccape, precipTotal]
      sky:        [tcc, lcc, mcc, hcc, refd1km]
      severe:     [srh3, ehi3, mlcape, mucape, mlcin, sbcin, liftedIdx, lapse75, stormMotion, shearSfc500]
      enviro:     [pwat, frzLvl, rh2m, vis, snod, ptype, precip6h]
      upper-wind: [wind925, wind850, wind700, wind500, wind300, wind250, wind200]
      upper-dyn:  [rh925, rh850, rh700, rh500, vort500, vv700]
    # Global; we still render CONUS for app-side overlay parity. App can request
    # a global bbox later by adding a "GFS_GLOBAL" model entry.
    bbox_lonlat: [-134.0, 21.0, -60.0, 53.0]
    image_size: [1024, 1024]
    # Same grid as HRRR so the app-side decimation logic is model-agnostic.
    # GFS is 0.25° native, so 128 samples across 74° stays honest (no
    # sub-native oversampling worth worrying about).
    point_grid: [128, 128]
    # Native GFS output is 3-hourly ALL the way to f384 (verified
    # 2026-08-06: f243/f252/f384 exist, f390 404). Segments render the
    # familiar 3-hourly cadence through f120, then 6-hourly to f384 —
    # the full 16-day range with no near-term regression and half the
    # frames a uniform 3-hourly extension would cost. _default stays as
    # the fallback for anything not segment-aware.
    forecast_hours_default: { start: 0, end: 120, step: 3 }
    forecast_hours_segments:
      - { start: 0, end: 120, step: 3 }
      - { start: 126, end: 384, step: 6 }
    s3_bucket: "noaa-gfs-bdp-pds"
    s3_key_template: "gfs.{date}/{run:02d}/atmos/gfs.t{run:02d}z.pgrb2.0p25.f{fh:03d}"
    display: "GFS 25km"
    picker_group: "Global"
    picker_order: 20
    runs_per_day: 4
    runs: [0, 6, 12, 18]
    retain_runs: 5
